# CodeRisk Cloud — 项目完整代码与问题分析文档

> **日期：** 2026-08-23  
> **项目：** CodeRisk Cloud（DevNetwork [API + Cloud + AI] Hackathon 参赛项目）  
> **仓库：** https://github.com/a9320/coderisk-cloud  
> **目标：** Overall Winner ($12,500) + Nutrient DWS Challenge ($1,500)

---

## 一、项目架构总览

### 1.1 技术栈

| 层级 | 技术 | 用途 |
|------|------|------|
| API 层 | FastAPI + Uvicorn | RESTful API，接收分析请求 |
| 任务队列 | Celery + Redis | 异步任务执行，解耦 API 和分析 |
| 分析引擎 | code-risk-agent（4 Agent 架构） | 代码安全分析核心 |
| 前端 | Streamlit Dashboard | 可视化展示分析结果 |
| PDF | Nutrient DWS API | 生成带数字签名的 PDF 报告 |
| 部署 | Docker Compose（4 容器） | redis / api / worker / dashboard |

### 1.2 容器架构

```
┌─────────────────────────────────────────────────────────┐
│                   docker-compose.yml                     │
│                                                         │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌─────────┐ │
│  │  redis    │  │   api    │  │  worker  │  │dashboard│ │
│  │ :6379    │  │  :8000   │  │  celery  │  │ :8501   │ │
│  │          │  │  FastAPI  │  │  4 Agent │  │Streamlit│ │
│  └──────────┘  └──────────┘  └──────────┘  └─────────┘ │
│       ▲              │              │            │       │
│       └──────────────┴──────────────┴────────────┘       │
│                    Redis 作为消息总线                      │
└─────────────────────────────────────────────────────────┘
```

### 1.3 代码目录结构

```
coderisk-cloud/
├── app/                          # Cloud API 层
│   ├── main.py                   # FastAPI 路由（444 行）
│   ├── tasks.py                  # Celery 任务定义（567 行）
│   ├── config.py                 # 配置管理（49 行）
│   ├── models.py                 # Pydantic 数据模型（118 行）
│   ├── dashboard.py              # Streamlit 前端（497 行）
│   ├── demo_fixture.py           # DEMO 预设数据（150 行）
│   └── nutrient_client.py        # Nutrient DWS 客户端（429 行）
│
├── code-risk-agent/              # 分析引擎（4 Agent 架构）
│   ├── orchestrator.py           # 状态机编排器（370 行）
│   ├── agents/
│   │   ├── static_analyzer.py    # Agent 1: 静态分析（472 行）
│   │   ├── semantic_analyzer.py  # Agent 2: LLM 语义分析（241 行）
│   │   ├── deep_verifier.py      # Agent 3: 深度验证（309 行）
│   │   └── report_generator.py   # Agent 4: 报告生成（467 行）
│   ├── core/
│   │   ├── models.py             # 核心数据模型（244 行）
│   │   ├── llm_client.py         # LLM 客户端（424 行）
│   │   ├── taint_analyzer.py     # 污点分析（210 行）
│   │   ├── semgrep_runner.py     # Semgrep 集成（136 行）
│   │   ├── cve_client.py         # CVE 数据库查询（167 行）
│   │   ├── memory.py             # 记忆层（251 行）
│   │   ├── dependency_scanner.py # 依赖扫描（373 行）
│   │   ├── attack_knowledge.py   # MITRE ATT&CK 映射（226 行）
│   │   └── retry.py              # 重试机制（59 行）
│   └── scripts/
│       ├── download_cve_data.py  # CVE 数据库构建脚本
│       └── download_osv_data.py  # OSV 数据下载脚本
│
├── docker-compose.yml            # 容器编排（65 行）
├── Dockerfile                    # 统一镜像（50 行）
├── requirements.txt              # Python 依赖
├── bruno/                        # API 测试集合
├── evidence/                     # 证据材料
├── reports/                      # 扫描报告输出
├── repos/                        # 待扫描的代码仓库
│   └── Damn-Vulnerable-Flask-Application/
└── 优化建议/                      # Kimi 历史优化方案（31 批次）
```

**总代码量：约 6,333 行**

---

## 二、4 Agent 分析流水线

### 2.1 流水线架构

```
代码输入 → Agent 1（静态分析）→ Agent 2（语义分析）→ Agent 3（深度验证）→ Agent 4（报告生成）
              │                      │                      │                      │
              ├─ Tree-sitter AST     ├─ LLM 验证+发现       ├─ 三重交叉验证         ├─ JSON/Markdown/SARIF
              ├─ 正则模式匹配        ├─ 攻击场景分析         ├─ CVE/CWE 知识库      ├─ PDF (Nutrient DWS)
              ├─ Semgrep 集成        ├─ 误报过滤            ├─ 记忆层（学习）       ├─ ATT&CK 映射
              └─ 依赖扫描            └─ 新漏洞发现          └─ 自省循环            └─ 终端 Rich 输出
```

### 2.2 Agent 1：静态分析器（StaticAnalyzer）

**文件：** `code-risk-agent/agents/static_analyzer.py`（472 行）

**功能：**
- C 语言危险函数检测（8 个：gets/strcpy/strcat/sprintf/scanf/system/memcpy/popen）
- C 语言漏洞模式（malloc 未检查 NULL、double free）
- C 新增模式（路径遍历、弱加密、整数溢出）
- Python 危险调用检测（10 个：eval/exec/pickle.loads/subprocess.call/os.system/yaml.load/xml.etree/tempfile.mktemp/hashlib.md5/hashlib.sha1）
- Python 漏洞模式（文件写入未验证、assert 用于安全检查）
- Python 新增模式（硬编码密码、硬编码 token）

**支持语言：** C、Python  
**检测方式：** 逐行正则匹配

### 2.3 Agent 2：语义分析器（SemanticAnalyzer）

**文件：** `code-risk-agent/agents/semantic_analyzer.py`（241 行）

**功能：**
- 接收 Agent 1 的风险列表，用 LLM 深度验证
- 判断 TRUE POSITIVE / FALSE POSITIVE
- 发现 Agent 1 遗漏的漏洞
- 输出攻击场景、影响分析、严重性调整

**依赖：** 需要 LLMClient（llama-cpp-python 或 llama-server）

### 2.4 Agent 3：深度验证器（DeepVerifier）

**文件：** `code-risk-agent/agents/deep_verifier.py`（309 行）

**功能：**
- **三重交叉验证：**
  1. 工具交叉：Semgrep + 模式匹配 + LLM 确认
  2. 知识库交叉：CWE/CVE 查询（本地 SQLite）
  3. 记忆交叉：已知误报抑制、已知真报提权
- **自省循环：** 最多 2 轮，让 Agent 3 发现前两个 Agent 遗漏的漏洞
- **CVE 预取：** 批量查询高危 CWE 的 CVE 数据

### 2.5 Agent 4：报告生成器（ReportGenerator）

**文件：** `code-risk-agent/agents/report_generator.py`（467 行）

**输出格式：**
- **JSON** — 结构化报告，含 CWE/CVE URL、ATT&CK 映射、合规引用
- **Markdown** — 人类可读报告，含表格、链接、修复建议
- **SARIF 2.1.0** — OASIS 标准，兼容 GitHub Code Scanning / VS Code / Azure DevOps
- **Rich Terminal** — 彩色终端输出，含风险摘要表、修复建议树

### 2.6 核心支撑模块

| 模块 | 文件 | 功能 |
|------|------|------|
| TaintAnalyzer | `core/taint_analyzer.py` | 污点分析：追踪 source→sink 数据流 |
| SemgrepRunner | `core/semgrep_runner.py` | Semgrep CLI 封装，转 Risk 对象 |
| CVEClient | `core/cve_client.py` | 本地 SQLite CVE 数据库查询 |
| MemoryLayer | `core/memory.py` | 双层记忆：正确记忆+错误记忆 |
| DependencyScanner | `core/dependency_scanner.py` | requirements.txt/package.json 依赖漏洞扫描 |
| AttackKnowledge | `core/attack_knowledge.py` | MITRE ATT&CK 技术→CWE 映射 |
| LLMClient | `core/llm_client.py` | 双后端：llama-cpp-python / llama-server HTTP |

---

## 三、Cloud API 层

### 3.1 API 端点

| 端点 | 方法 | 功能 | 认证 |
|------|------|------|------|
| `/api/v1/analyze` | POST | GitHub 仓库分析 | API Key |
| `/api/v1/analyze/upload` | POST | ZIP 文件上传分析 | API Key |
| `/api/v1/scan-local` | POST | 本地目录扫描 | API Key |
| `/api/v1/tasks/{task_id}` | GET | 查询任务状态 | API Key |
| `/api/v1/reports/{task_id}` | GET | 获取分析报告 | API Key |
| `/api/v1/reports/{task_id}/pdf` | GET | 下载 PDF 报告 | API Key |
| `/api/v1/webhooks/github` | POST | GitHub Push Webhook | Webhook Secret |
| `/health` | GET | 健康检查 | 无 |
| `/demo` | GET | DEMO 数据 | 无 |

### 3.2 安全机制

- **API Key 认证：** `hmac.compare_digest` 防时序攻击
- **报告隔离：** API Key hash 绑定，不同 Key 不能互相访问报告
- **路径遍历防护：** 三层校验（normpath + `..` 检测 + `/repos/` 前缀）
- **ZIP 炸弹防护：** 解压大小 ≤100MB + 文件数 ≤10,000 + 路径遍历检测
- **GitHub Webhook：** HMAC-SHA256 签名验证
- **CORS：** `allow_origins=["*"]`（开发阶段，生产需收紧）

### 3.3 Celery 任务

**文件：** `app/tasks.py`（567 行）

**任务流程：**
1. `_prepare_code()` — 准备代码（Git clone / ZIP 解压 / 本地目录）
2. `_run_static_analysis()` — Agent 1 静态分析
3. `_run_semantic_analysis()` — Agent 2 语义分析
4. `_run_verification()` — Agent 3 深度验证
5. `_generate_report()` — Agent 4 报告生成（JSON + SARIF + PDF）

---

## 四、已发现的问题与修复记录

### 4.1 已修复的问题（2026-08-23）

#### 问题 1：Docker 容器内 Python path 缺失

**现象：** `ModuleNotFoundError: No module named 'orchestrator'`

**原因：** `orchestrator.py` 在 `/app/code-risk-agent/`，但 Python path 只有 `/app`，且 `code-risk-agent` 带连字符不能当包名 import。

**修复：** `docker-compose.yml` worker 环境加 `PYTHONPATH=/app/code-risk-agent`

---

#### 问题 2：SemanticAnalyzer 初始化缺 llm_client

**现象：** `SemanticAnalyzer.__init__() missing 1 required positional argument: 'llm_client'`

**原因：** `tasks.py` 中 `_get_semantic_analyzer()` 调用 `SemanticAnalyzer()` 未传参数。

**修复：** 新增 `_get_llm_client()` 懒加载，传给 `SemanticAnalyzer(llm)`。

---

#### 问题 3：StaticAnalyzer.analyze() 参数类型错误

**现象：** `'str' object has no attribute 'language'`

**原因：** `tasks.py` 传字符串路径给 `analyzer.analyze(code_path)`，但方法期望 `CodeFile` 对象。

**修复：** 新增 `_scan_code_files()` 函数，扫描目录生成 `CodeFile` 对象列表，调用 `analyze_batch()`。

---

#### 问题 4：DeepVerifier 方法名错误

**现象：** `'DeepVerifier' object has no attribute 'verify'`

**原因：** `tasks.py` 调用 `verifier.verify(code_path, findings)`，但方法名是 `verify_batch(files, risks)`。

**修复：** 改用 `verify_batch()`，并将 dict findings 转回 `Risk` 对象。

---

#### 问题 5：Risk 模型字段不匹配

**现象：** `Risk` 构造时字段名错误（`file` → `file_path`，`line` → `line_start`/`line_end`）

**原因：** `tasks.py` 中 dict→Risk 转换使用了错误的字段名。

**修复：** 对齐 `Risk` 模型定义，使用正确的字段名和类型（`Confidence` 枚举不是数字）。

---

### 4.2 当前存在的问题

#### 🔴 严重问题

##### P1：静态分析器只支持 C 和 Python，不支持 Web 框架

**现状：** `static_analyzer.py` 的 `_detect_language()` 只识别 `.c`/`.h`/`.py`，Flask/Django/JS 等 Web 应用的漏洞模式完全缺失。

**影响：** 扫描 Damn-Vulnerable-Flask-Application 只找到 1 个 CRITICAL（eval），实际应有 10+ 个漏洞（SQL 注入、XSS、命令注入、路径遍历等）。

**需要添加的规则：**
- Flask/Jinja2 SSTI（Server-Side Template Injection）
- SQL 注入（`db.execute()` + 字符串拼接）
- XSS（`render_template_string()`、未转义的 `|safe`）
- 命令注入（`subprocess.call(shell=True)`、`os.popen()`）
- 路径遍历（`send_file()` + 用户输入）
- 不安全的 Session 配置（`SECRET_KEY` 硬编码）
- CORS 配置过松
- Debug 模式开启

---

##### P2：语义分析（Agent 2）完全不可用

**现状：** 容器内没有 `llama-cpp-python`，也没有 GGUF 模型文件。Agent 2 被跳过。

**影响：** 只有静态模式匹配，没有 LLM 深度分析，漏报率极高。

**解决方案（二选一）：**
1. **容器内装 llama-cpp-python + 下载 Q4 量化模型** — 体积大（~4GB），构建慢
2. **用远程 LLM API 替代** — 改 `LLMClient` 支持 OpenAI 兼容 API（如 MiniMax、DeepSeek）

---

##### P3：CVE 数据库未构建

**现状：** `/app/code-risk-agent/data/vuln_db.sqlite` 不存在，CVE 查询返回空。

**影响：** Agent 3 的知识库交叉验证失效。

**修复：** 在 Dockerfile 中加 `RUN python scripts/download_cve_data.py`，或挂载预构建的数据库。

---

##### P4：Semgrep 未安装

**现状：** 容器内没有 `semgrep` CLI。Semgrep 分析被跳过。

**影响：** 少了一层重要的静态分析。

**修复：** `requirements.txt` 加 `semgrep`，或 Dockerfile 加 `pip install semgrep`。

---

#### 🟡 中等问题

##### P5：TaintAnalyzer 未被 tasks.py 调用

**现状：** `orchestrator.py` 中有污点分析调用，但 `tasks.py` 的 Celery 任务没有调用 `TaintAnalyzer`。

**影响：** source→sink 数据流追踪完全缺失。

**修复：** 在 `_run_static_analysis()` 之后加污点分析调用。

---

##### P6：dependency_scanner 未被 tasks.py 调用

**现状：** `orchestrator.py` 中有依赖扫描，但 `tasks.py` 没有。

**影响：** 不检测项目依赖中的已知漏洞。

**修复：** 在静态分析后加 `scan_project_dependencies()` 调用。

---

##### P7：DEMO 数据是硬编码的假数据

**现状：** `demo_fixture.py` 包含预设的 10 条 findings（CRITICAL 1, HIGH 2, MEDIUM 3, LOW 2, INFO 1），不是真实扫描结果。

**影响：** 评委如果对比 DEMO 和 LOCAL 结果，会发现差距巨大。

**建议：** 改为真实扫描 `/demo` 端点，或标注 "pre-computed demo results"。

---

##### P8：Dashboard 不支持实时进度更新

**现状：** `dashboard.py` 用 `st.rerun()` + `time.sleep(3)` 轮询，不是 WebSocket 推送。

**影响：** 用户体验差，刷新闪烁。

---

#### 🟢 次要问题

##### P9：CORS 配置过松

**现状：** `allow_origins=["*"]`，生产环境需收紧。

##### P10：报告只保存 JSON，SARIF/PDF 未实际写入

**现状：** `_generate_report()` 中 SARIF 和 PDF 逻辑存在，但 `output_formats` 默认只有 `["json", "sarif"]`，PDF 需要 Nutrient API Key。

##### P11：worker 容器以 root 运行

**现状：** Celery worker 以 superuser 身份运行，有安全警告。

##### P12：MemoryLayer 使用文件锁（fcntl）

**现状：** `memory.py` 用 `fcntl.flock()` 做并发控制，Windows 不兼容。但 Docker 内是 Linux，不影响当前部署。

---

## 五、扫描结果分析

### 5.1 最新扫描报告（cr-20260823-3796381d）

**目标：** Damn-Vulnerable-Flask-Application（本地目录）

```json
{
  "summary": {"critical": 1, "high": 0, "medium": 0, "low": 0, "info": 0},
  "findings": [
    {
      "id": "RISK-001",
      "type": "CWE-95",
      "title": "Code Injection via eval()",
      "severity": "critical",
      "file": "/repos/Damn-Vulnerable-Flask-Application/app.py",
      "line": 120,
      "confidence": 30
    }
  ],
  "total_findings": 1
}
```

### 5.2 问题分析

**只找到 1 个 CRITICAL，原因：**

1. **语言检测限制：** `.py` 文件被识别为 Python，但 Flask 应用的 Web 特有漏洞（SSTI、SQL 注入、XSS）不在检测规则中
2. **Agent 2 跳过：** 无 LLM，语义分析完全缺失
3. **Agent 3 降级：** 无 CVE 数据库，交叉验证只靠 pattern_match 一个来源
4. **Semgrep 跳过：** 未安装
5. **Taint 分析跳过：** 未调用
6. **依赖扫描跳过：** 未调用

**DVHA 应该检出的漏洞（参考）：**

| 漏洞 | 类型 | 文件 | 行号 | 当前是否检出 |
|------|------|------|------|-------------|
| eval() 代码注入 | CWE-95 | app.py | 120 | ✅ 已检出 |
| SQL 注入（字符串拼接） | CWE-89 | app.py | ~30 | ❌ 未检出 |
| XSS（未转义输出） | CWE-79 | templates/ | 多处 | ❌ 未检出 |
| 命令注入（os.popen） | CWE-78 | app.py | ~80 | ❌ 未检出 |
| 路径遍历（send_file） | CWE-22 | app.py | ~60 | ❌ 未检出 |
| 硬编码 SECRET_KEY | CWE-798 | app.py | ~15 | ❌ 未检出 |
| Debug 模式开启 | CWE-489 | app.py | 末尾 | ❌ 未检出 |
| 不安全的 Session | CWE-613 | app.py | 多处 | ❌ 未检出 |

---

## 六、竞品对比与差距分析

### 6.1 类似项目

| 项目 | Star | 特点 | CodeRisk 差距 |
|------|------|------|--------------|
| Semgrep | 10K+ | 规则丰富、CI/CD 集成 | 我们有 Semgrep 集成但未启用 |
| Bandit | 6K+ | Python 专用安全扫描 | 我们的 Python 规则覆盖不足 |
| CodeQL | GitHub 官方 | 深度语义分析 | 我们有 LLM 语义分析但不可用 |
| CAI (Cisco) | 新兴 | Multi-Agent 安全审计 | 架构类似，我们有记忆层 |

### 6.2 CodeRisk 的差异化卖点

1. **4 Agent 协作架构** — 不是单工具，是多 Agent 流水线
2. **记忆层学习** — 越用越准（正确记忆 + 错误记忆）
3. **本地 GPU 推理** — 隐私优先，代码不离开本地
4. **多格式报告** — JSON/Markdown/SARIF/PDF
5. **MITRE ATT&CK 映射** — 企业级合规参考
6. **Nutrient DWS 签名 PDF** — 满足 Nutrient DWS Challenge 要求

---

## 七、优先级修复建议

### 🔥 Day 7-8（立即）

| 优先级 | 任务 | 预计耗时 | 影响 |
|--------|------|----------|------|
| P1 | 扩展静态分析规则（Flask/Web 漏洞模式） | 2-3h | 直接提升检出率 |
| P4 | 容器内安装 Semgrep | 30min | 多一层分析 |
| P3 | 构建 CVE 数据库（Dockerfile 内） | 30min | Agent 3 交叉验证生效 |
| P5+P6 | tasks.py 调用 TaintAnalyzer + DependencyScanner | 1h | 补全流水线 |

### ⚡ Day 9-10

| 优先级 | 任务 | 预计耗时 | 影响 |
|--------|------|----------|------|
| P2 | 接入远程 LLM API（DeepSeek/MiniMax） | 2h | Agent 2 语义分析可用 |
| P7 | DEMO 端点改为真实扫描 | 1h | 评委体验一致 |
| P8 | Dashboard 实时进度（WebSocket/SSE） | 2h | 用户体验提升 |

### 📋 Day 11-14

| 优先级 | 任务 | 预计耗时 | 影响 |
|--------|------|----------|------|
| P9 | CORS 收紧 + 安全加固 | 1h | 生产就绪 |
| P11 | 非 root 用户运行 worker | 30min | 安全合规 |
| 完善文档 | README + API 文档 + 架构图 | 3h | 评委理解项目 |
| Demo 视频 | 录制完整扫描演示 | 2h | 提交材料 |

---

## 八、Docker 部署快速参考

### 8.1 构建与启动

```powershell
cd D:\desk-top\coderisk-cloud
docker compose build --no-cache
docker compose up -d
```

### 8.2 验证

```powershell
# 健康检查
curl http://localhost:8000/health

# DEMO 数据
curl http://localhost:8000/demo

# 本地扫描
curl -X POST http://localhost:8000/api/v1/scan-local \
  -H "Authorization: Bearer dev-key-change-in-production" \
  -H "Content-Type: application/json" \
  -d '{"local_path": "/repos/Damn-Vulnerable-Flask-Application"}'

# 查看任务状态
curl http://localhost:8000/api/v1/tasks/{task_id} \
  -H "Authorization: Bearer dev-key-change-in-production"
```

### 8.3 日志查看

```powershell
docker logs coderisk-worker --tail 50
docker logs coderisk-api --tail 50
```

---

## 九、环境变量配置

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `REDIS_URL` | `redis://redis:6379/0` | Redis 连接地址 |
| `CODERISK_API_KEY` | `dev-key-change-in-production` | API 认证密钥 |
| `CODERISK_PATH` | `/app/code-risk-agent` | 分析引擎路径 |
| `NUTRIENT_DWS_API_KEY` | （空） | Nutrient PDF API Key |
| `REPORTS_DIR` | `/app/reports` | 报告输出目录 |
| `WORKER_CONCURRENCY` | `2` | Celery 并发数 |
| `LLM_BACKEND` | `local_llama_cpp` | LLM 后端类型 |
| `CODERISK_MODEL_PATH` | （自动探测） | GGUF 模型路径 |
| `GITHUB_WEBHOOK_SECRET` | （空） | GitHub Webhook 签名密钥 |
| `SEMGREP_RULES` | `p/default` | Semgrep 规则集 |

---

## 十、关键文件索引

| 文件 | 行数 | 核心功能 |
|------|------|----------|
| `app/main.py` | 444 | FastAPI 路由、认证、Webhook |
| `app/tasks.py` | 567 | Celery 任务、Agent 调用编排 |
| `app/dashboard.py` | 497 | Streamlit 前端界面 |
| `app/nutrient_client.py` | 429 | Nutrient DWS PDF 生成+签名 |
| `code-risk-agent/orchestrator.py` | 370 | 状态机编排器（CLI 版） |
| `code-risk-agent/agents/static_analyzer.py` | 472 | Agent 1: 模式匹配 |
| `code-risk-agent/agents/semantic_analyzer.py` | 241 | Agent 2: LLM 分析 |
| `code-risk-agent/agents/deep_verifier.py` | 309 | Agent 3: 交叉验证 |
| `code-risk-agent/agents/report_generator.py` | 467 | Agent 4: 报告生成 |
| `code-risk-agent/core/models.py` | 244 | 核心数据模型定义 |
| `code-risk-agent/core/llm_client.py` | 424 | LLM 双后端客户端 |
| `code-risk-agent/core/taint_analyzer.py` | 210 | 污点分析引擎 |
| `code-risk-agent/core/memory.py` | 251 | 记忆层（正确+错误） |
| `code-risk-agent/core/dependency_scanner.py` | 373 | 依赖漏洞扫描 |
| `code-risk-agent/core/attack_knowledge.py` | 226 | ATT&CK 映射 |
| `docker-compose.yml` | 65 | 容器编排 |
| `Dockerfile` | 50 | 统一镜像定义 |

---

*文档生成时间：2026-08-23 20:30*  
*生成工具：lolo（OpenClaw Agent）*
