# CodeRisk Cloud — 项目完整文档

> DevNetwork [API + Cloud + AI] Hackathon 2026 参赛作品
> 版本：v0.1（Day 1 完成）| 日期：2026-08-17

---

## 一、项目概述

### 1.1 定位

CodeRisk Cloud 是 [CodeRisk Agent](https://github.com/a9320/code-risk-agent) 的 Cloud-Native API 版本，将本地 CLI 代码安全审计工具升级为 REST API 服务。

**一句话定位：** "唯一能在本地 AMD GPU 上运行 LLM 推理的代码安全 API — 源代码永不离开你的基础设施。"

### 1.2 核心卖点

| 卖点 | 说明 |
|------|------|
| **本地 GPU 推理** | LLM 运行在 AMD MI300X（192GB HBM3），代码不上传云端 |
| **4-Agent 流水线** | 静态分析 → 语义理解 → 深度验证 → 报告生成 |
| **Nutrient DWS** | 专业 PDF 审计报告 + 数字签名 + 在线预览 |
| **GitHub Actions** | Push 触发分析，PR 评论显示漏洞摘要 |

### 1.3 技术栈

| 层 | 组件 | 技术 |
|----|------|------|
| Client | GitHub Actions / Dashboard | GitHub Webhook, React |
| Gateway | API 网关 | FastAPI + Uvicorn |
| Queue | 任务队列 | Celery + Redis |
| Worker | 分析引擎 | CodeRisk Agent（4-Agent） |
| LLM | 语义分析 | Qwen2.5-Coder-32B（AMD MI300X） |
| Document | 报告生成 | Nutrient DWS（PDF + 签名） |

---

## 二、项目结构

```
coderisk-cloud/
├── app/
│   ├── __init__.py          (1行)   — 包初始化
│   ├── config.py            (37行)  — 配置管理
│   ├── main.py              (226行) — FastAPI 主应用（5个端点）
│   ├── models.py            (111行) — Pydantic 数据模型
│   └── tasks.py             (306行) — Celery 任务 + 4-Agent 调用
├── tests/                           — 测试（待补充）
├── reports/                         — 报告输出目录
├── .env.example             (19行)  — 环境变量模板
├── requirements.txt         (8行)   — Python 依赖
└── README.md                (88行)  — 项目说明
```

**总代码量：** 796 行（5 个 Python 文件）

---

## 三、API 设计

### 3.1 端点列表

| 方法 | 路径 | 功能 | 认证 |
|------|------|------|------|
| POST | `/api/v1/analyze` | 提交分析任务 | Bearer Token |
| GET | `/api/v1/tasks/{task_id}` | 查询任务状态 | Bearer Token |
| GET | `/api/v1/reports/{task_id}` | 获取分析报告 | Bearer Token |
| GET | `/health` | 健康检查 | 无 |
| GET | `/docs` | Swagger 文档 | 无 |

### 3.2 POST /api/v1/analyze

**请求：**
```json
{
  "source": "github",           // github / zip / direct_upload
  "repo_url": "https://github.com/user/repo",
  "branch": "main",
  "callback_url": "https://...",
  "output_formats": ["json", "sarif", "pdf"]
}
```

**响应：**
```json
{
  "task_id": "cr-20260817-abc12345",
  "status": "pending",
  "message": "Analysis task created. Use GET /api/v1/tasks/cr-20260817-abc12345 to check progress."
}
```

### 3.3 GET /api/v1/tasks/{task_id}

**响应：**
```json
{
  "task_id": "cr-20260817-abc12345",
  "status": "completed",
  "progress": 100,
  "agent_status": {
    "agent_1_static": "completed",
    "agent_2_semantic": "completed",
    "agent_3_verifier": "completed",
    "agent_4_report": "completed"
  },
  "created_at": "2026-08-17T21:20:25",
  "updated_at": "2026-08-17T21:20:28",
  "error": null
}
```

**状态机：**
```
pending → queued → analyzing → verifying → generating_report → completed
                                                                  ↓
                                                              failed
```

### 3.4 GET /api/v1/reports/{task_id}

**响应：**
```json
{
  "task_id": "cr-20260817-abc12345",
  "summary": {"critical": 3, "high": 5, "medium": 8, "low": 2, "info": 1},
  "findings": [...],
  "report_urls": {
    "json": "/reports/cr-001/report.json",
    "sarif": "/reports/cr-001/report.sarif",
    "pdf": "/reports/cr-001/report.pdf"
  },
  "digital_signature": "sha256:...",
  "completed_at": "2026-08-17T21:20:28"
}
```

### 3.5 认证

所有 API 端点（除 /health 和 /docs）需要 Bearer Token 认证：

```bash
curl -H "Authorization: Bearer your-api-key" http://localhost:8000/api/v1/...
```

API Key 通过环境变量 `CODERISK_API_KEY` 配置。

---

## 四、核心代码

### 4.1 config.py — 配置管理

```python
"""CodeRisk Cloud — 配置管理"""

import os
from pathlib import Path


class Settings:
    # API
    API_TITLE = "CodeRisk Cloud"
    API_VERSION = "1.0.0"
    API_PREFIX = "/api/v1"

    # Redis (Celery Broker + Backend)
    REDIS_URL = os.getenv("REDIS_URL", "redis://localhost:6379/0")

    # CodeRisk Agent
    CODERISK_PATH = os.getenv(
        "CODERISK_PATH",
        str(Path(__file__).parent.parent.parent / "天禧AI Skills苍穹共创计划" / "code-risk-agent"),
    )

    # Nutrient DWS
    NUTRIENT_API_KEY = os.getenv("NUTRIENT_DWS_API_KEY", "")
    NUTRIENT_API_URL = "https://api.nutrient.io/build"

    # Worker
    WORKER_CONCURRENCY = int(os.getenv("WORKER_CONCURRENCY", "2"))

    # Auth
    API_KEY = os.getenv("CODERISK_API_KEY", "dev-key-change-in-production")

    # Storage
    REPORTS_DIR = Path(os.getenv("REPORTS_DIR", str(Path(__file__).parent.parent / "reports")))
    REPORTS_DIR.mkdir(parents=True, exist_ok=True)


settings = Settings()
```

### 4.2 models.py — Pydantic 数据模型

```python
"""CodeRisk Cloud — Pydantic 数据模型"""

from __future__ import annotations

from datetime import datetime
from enum import Enum
from typing import Any, Optional

from pydantic import BaseModel, Field


# ── 请求模型 ──

class AnalyzeRequest(BaseModel):
    """分析请求"""
    source: str = Field(
        default="direct_upload",
        description="代码来源: github / zip / direct_upload",
    )
    repo_url: Optional[str] = Field(
        default=None,
        description="GitHub 仓库 URL（source=github 时必填）",
    )
    branch: str = Field(default="main", description="Git 分支")
    callback_url: Optional[str] = Field(default=None, description="回调 URL")
    output_formats: list[str] = Field(
        default=["json", "sarif"],
        description="输出格式: json / sarif / pdf",
    )


# ── 响应模型 ──

class TaskStatus(str, Enum):
    PENDING = "pending"
    QUEUED = "queued"
    ANALYZING = "analyzing"
    VERIFYING = "verifying"
    GENERATING_REPORT = "generating_report"
    COMPLETED = "completed"
    FAILED = "failed"


class AgentStatus(BaseModel):
    """各 Agent 的执行状态"""
    agent_1_static: str = "pending"
    agent_2_semantic: str = "pending"
    agent_3_verifier: str = "pending"
    agent_4_report: str = "pending"


class TaskResponse(BaseModel):
    """任务状态响应"""
    task_id: str
    status: TaskStatus
    progress: int = Field(ge=0, le=100)
    agent_status: AgentStatus = Field(default_factory=AgentStatus)
    created_at: Optional[str] = None
    updated_at: Optional[str] = None
    error: Optional[str] = None


class FindingSummary(BaseModel):
    critical: int = 0
    high: int = 0
    medium: int = 0
    low: int = 0
    info: int = 0


class ReportURLs(BaseModel):
    json_url: Optional[str] = Field(default=None, alias="json")
    sarif_url: Optional[str] = Field(default=None, alias="sarif")
    pdf_url: Optional[str] = Field(default=None, alias="pdf")
    model_config = {"populate_by_name": True}


class ReportResponse(BaseModel):
    task_id: str
    summary: FindingSummary = Field(default_factory=FindingSummary)
    findings: list[dict[str, Any]] = Field(default_factory=list)
    report_urls: ReportURLs = Field(default_factory=ReportURLs)
    digital_signature: Optional[str] = None
    completed_at: Optional[str] = None


class AnalyzeResponse(BaseModel):
    task_id: str
    status: TaskStatus = TaskStatus.PENDING
    message: str = "Analysis task created successfully"


class ErrorResponse(BaseModel):
    error: str
    detail: Optional[str] = None
```

### 4.3 tasks.py — Celery 任务 + 4-Agent 调用

```python
"""CodeRisk Cloud — Celery 任务定义"""

from __future__ import annotations

import json
import sys
import time
import uuid
from datetime import datetime
from pathlib import Path
from typing import Any

from celery import Celery

from app.config import settings

# ── Celery 应用 ──

celery_app = Celery(
    "coderisk",
    broker=settings.REDIS_URL,
    backend=settings.REDIS_URL,
)

celery_app.conf.update(
    task_serializer="json",
    accept_content=["json"],
    result_serializer="json",
    timezone="Asia/Shanghai",
    enable_utc=True,
    task_track_started=True,
    worker_concurrency=settings.WORKER_CONCURRENCY,
)


# ── 进度更新 ──

def _update_progress(task_id: str, status: str, progress: int, agent_status: dict | None = None):
    """更新任务进度到 Redis"""
    import redis
    r = redis.from_url(settings.REDIS_URL)
    data = {
        "task_id": task_id,
        "status": status,
        "progress": progress,
        "agent_status": agent_status or {},
        "updated_at": datetime.now().isoformat(),
    }
    r.set(f"task:{task_id}:status", json.dumps(data), ex=86400)


# ── 核心分析任务 ──

@celery_app.task(bind=True, name="analyze_codebase")
def analyze_codebase_task(self, task_id: str, source_config: dict[str, Any]) -> dict:
    """
    执行代码安全分析。

    流程：
      1. 获取代码（Git clone / 解压 ZIP / 读取上传文件）
      2. Agent 1: 静态分析（Semgrep）
      3. Agent 2: 语义分析（LLM）
      4. Agent 3: 深度验证（交叉验证）
      5. Agent 4: 报告生成（JSON/SARIF/PDF）
    """
    try:
        # 阶段 1: 准备代码
        _update_progress(task_id, "analyzing", 5, {
            "agent_1_static": "preparing",
            "agent_2_semantic": "pending",
            "agent_3_verifier": "pending",
            "agent_4_report": "pending",
        })
        code_path = _prepare_code(task_id, source_config)
        if not code_path:
            raise ValueError("Failed to prepare source code")

        # 阶段 2: Agent 1 — 静态分析
        _update_progress(task_id, "analyzing", 20, {
            "agent_1_static": "running", "agent_2_semantic": "pending",
            "agent_3_verifier": "pending", "agent_4_report": "pending",
        })
        static_findings = _run_static_analysis(task_id, code_path)
        _update_progress(task_id, "analyzing", 40, {
            "agent_1_static": "completed", "agent_2_semantic": "pending",
            "agent_3_verifier": "pending", "agent_4_report": "pending",
        })

        # 阶段 3: Agent 2 — 语义分析
        _update_progress(task_id, "analyzing", 50, {
            "agent_1_static": "completed", "agent_2_semantic": "running",
            "agent_3_verifier": "pending", "agent_4_report": "pending",
        })
        semantic_findings = _run_semantic_analysis(task_id, code_path, static_findings)
        _update_progress(task_id, "analyzing", 70, {
            "agent_1_static": "completed", "agent_2_semantic": "completed",
            "agent_3_verifier": "pending", "agent_4_report": "pending",
        })

        # 阶段 4: Agent 3 — 深度验证
        _update_progress(task_id, "verifying", 80, {
            "agent_1_static": "completed", "agent_2_semantic": "completed",
            "agent_3_verifier": "running", "agent_4_report": "pending",
        })
        verified_findings = _run_verification(task_id, code_path, semantic_findings)
        _update_progress(task_id, "verifying", 90, {
            "agent_1_static": "completed", "agent_2_semantic": "completed",
            "agent_3_verifier": "completed", "agent_4_report": "pending",
        })

        # 阶段 5: Agent 4 — 报告生成
        _update_progress(task_id, "generating_report", 95, {
            "agent_1_static": "completed", "agent_2_semantic": "completed",
            "agent_3_verifier": "completed", "agent_4_report": "running",
        })
        report = _generate_report(task_id, verified_findings, source_config.get("output_formats", ["json"]))
        _update_progress(task_id, "completed", 100, {
            "agent_1_static": "completed", "agent_2_semantic": "completed",
            "agent_3_verifier": "completed", "agent_4_report": "completed",
        })

        # 保存结果
        result_path = settings.REPORTS_DIR / f"{task_id}.json"
        result_path.write_text(json.dumps(report, ensure_ascii=False, indent=2), encoding="utf-8")

        return {"task_id": task_id, "status": "completed", "report": report}

    except Exception as e:
        _update_progress(task_id, "failed", 0, {
            "agent_1_static": "error", "agent_2_semantic": "error",
            "agent_3_verifier": "error", "agent_4_report": "error",
        })
        return {"task_id": task_id, "status": "failed", "error": str(e)}


# ── 内部实现 ──

def _prepare_code(task_id: str, config: dict) -> str | None:
    """准备代码：clone / 解压 / 读取"""
    import tempfile
    work_dir = Path(tempfile.mkdtemp(prefix=f"cr-{task_id}-"))
    source = config.get("source", "direct_upload")

    if source == "github":
        repo_url = config.get("repo_url", "")
        branch = config.get("branch", "main")
        if not repo_url:
            return None
        import subprocess
        try:
            subprocess.run(
                ["git", "clone", "--depth", "1", "-b", branch, repo_url, str(work_dir)],
                capture_output=True, timeout=120, check=True,
            )
            return str(work_dir)
        except Exception:
            return None
    elif source == "direct_upload":
        return str(work_dir)
    return None


def _run_static_analysis(task_id: str, code_path: str) -> list[dict]:
    """Agent 1: 静态分析（Semgrep）"""
    sys.path.insert(0, settings.CODERISK_PATH)
    try:
        from agents.static_analyzer import StaticAnalyzer
        analyzer = StaticAnalyzer()
        results = analyzer.analyze(code_path)
        return results if isinstance(results, list) else []
    except Exception as e:
        return [{"type": "error", "message": f"Static analysis failed: {e}"}]


def _run_semantic_analysis(task_id: str, code_path: str, static_findings: list[dict]) -> list[dict]:
    """Agent 2: 语义分析（LLM）"""
    sys.path.insert(0, settings.CODERISK_PATH)
    try:
        from agents.semantic_analyzer import SemanticAnalyzer
        analyzer = SemanticAnalyzer()
        results = analyzer.analyze(code_path, static_findings)
        return results if isinstance(results, list) else static_findings
    except Exception as e:
        return static_findings


def _run_verification(task_id: str, code_path: str, findings: list[dict]) -> list[dict]:
    """Agent 3: 深度验证（交叉验证）"""
    sys.path.insert(0, settings.CODERISK_PATH)
    try:
        from agents.deep_verifier import DeepVerifier
        verifier = DeepVerifier()
        results = verifier.verify(code_path, findings)
        return results if isinstance(results, list) else findings
    except Exception as e:
        return findings


def _generate_report(task_id: str, findings: list[dict], formats: list[str]) -> dict:
    """Agent 4: 报告生成"""
    summary = {"critical": 0, "high": 0, "medium": 0, "low": 0, "info": 0}
    for f in findings:
        sev = f.get("severity", "info").lower()
        if sev in summary:
            summary[sev] += 1

    report = {
        "task_id": task_id,
        "generated_at": datetime.now().isoformat(),
        "summary": summary,
        "findings": findings,
        "total_findings": len(findings),
    }

    if "sarif" in formats:
        sarif_path = settings.REPORTS_DIR / f"{task_id}.sarif"
        sarif = _to_sarif(findings)
        sarif_path.write_text(json.dumps(sarif, indent=2), encoding="utf-8")
        report["sarif_path"] = str(sarif_path)

    return report


def _to_sarif(findings: list[dict]) -> dict:
    """转换为 SARIF 格式"""
    results = []
    for f in findings:
        results.append({
            "ruleId": f.get("type", "unknown"),
            "level": _severity_to_sarif_level(f.get("severity", "info")),
            "message": {"text": f.get("description", "")},
            "locations": [{
                "physicalLocation": {
                    "artifactLocation": {"uri": f.get("file", "")},
                    "region": {"startLine": f.get("line", 0)},
                }
            }],
        })
    return {
        "$schema": "https://raw.githubusercontent.com/oasis-tcs/sarif-spec/main/sarif-2.1/schema/sarif-schema-2.1.0.json",
        "version": "2.1.0",
        "runs": [{"tool": {"driver": {"name": "CodeRisk Cloud", "version": "1.0.0"}}, "results": results}],
    }


def _severity_to_sarif_level(severity: str) -> str:
    mapping = {"critical": "error", "high": "error", "medium": "warning", "low": "note", "info": "none"}
    return mapping.get(severity, "none")
```

### 4.4 main.py — FastAPI 主应用

```python
"""CodeRisk Cloud — FastAPI 主应用"""

from __future__ import annotations

import json
import uuid
from datetime import datetime
from pathlib import Path

import redis
from fastapi import FastAPI, Header, HTTPException
from fastapi.middleware.cors import CORSMiddleware

from app.config import settings
from app.models import (
    AnalyzeRequest, AnalyzeResponse, ErrorResponse,
    ReportResponse, TaskResponse, TaskStatus,
)
from app.tasks import analyze_codebase_task, celery_app

app = FastAPI(
    title=settings.API_TITLE,
    version=settings.API_VERSION,
    description="AI-powered code security API with local GPU inference",
)

app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

r = redis.from_url(settings.REDIS_URL)


def verify_api_key(authorization: str | None = Header(None)):
    """验证 API Key"""
    if not authorization:
        raise HTTPException(status_code=401, detail="Missing Authorization header")
    token = authorization.replace("Bearer ", "").strip()
    if token != settings.API_KEY:
        raise HTTPException(status_code=403, detail="Invalid API Key")
    return token


@app.post(f"{settings.API_PREFIX}/analyze", response_model=AnalyzeResponse)
async def analyze(request: AnalyzeRequest, authorization: str | None = Header(None)):
    verify_api_key(authorization)
    if request.source == "github" and not request.repo_url:
        raise HTTPException(status_code=400, detail="repo_url required when source=github")

    task_id = f"cr-{datetime.now().strftime('%Y%m%d')}-{uuid.uuid4().hex[:8]}"
    meta = {
        "task_id": task_id, "source": request.source,
        "repo_url": request.repo_url, "branch": request.branch,
        "output_formats": request.output_formats,
        "callback_url": request.callback_url,
        "created_at": datetime.now().isoformat(),
    }
    r.set(f"task:{task_id}:meta", json.dumps(meta), ex=86400)
    r.set(f"task:{task_id}:status", json.dumps({
        "task_id": task_id, "status": "pending", "progress": 0,
        "agent_status": {}, "updated_at": datetime.now().isoformat(),
    }), ex=86400)

    analyze_codebase_task.delay(task_id, meta)
    return AnalyzeResponse(task_id=task_id, status=TaskStatus.PENDING,
        message=f"Analysis task created. Use GET {settings.API_PREFIX}/tasks/{task_id} to check progress.")


@app.get(f"{settings.API_PREFIX}/tasks/{{task_id}}", response_model=TaskResponse)
async def get_task_status(task_id: str, authorization: str | None = Header(None)):
    verify_api_key(authorization)
    status_data = r.get(f"task:{task_id}:status")
    if not status_data:
        raise HTTPException(status_code=404, detail=f"Task {task_id} not found")
    data = json.loads(status_data)
    return TaskResponse(
        task_id=data["task_id"], status=TaskStatus(data["status"]),
        progress=data.get("progress", 0), agent_status=data.get("agent_status", {}),
        created_at=data.get("created_at"), updated_at=data.get("updated_at"),
        error=data.get("error"),
    )


@app.get(f"{settings.API_PREFIX}/reports/{{task_id}}", response_model=ReportResponse)
async def get_report(task_id: str, authorization: str | None = Header(None)):
    verify_api_key(authorization)
    status_data = r.get(f"task:{task_id}:status")
    if not status_data:
        raise HTTPException(status_code=404, detail=f"Task {task_id} not found")
    data = json.loads(status_data)
    if data["status"] != "completed":
        raise HTTPException(status_code=400, detail=f"Task not completed. Current status: {data['status']}")

    report_path = settings.REPORTS_DIR / f"{task_id}.json"
    if not report_path.exists():
        raise HTTPException(status_code=404, detail="Report file not found")
    report = json.loads(report_path.read_text(encoding="utf-8"))

    return ReportResponse(
        task_id=task_id, summary=report.get("summary", {}),
        findings=report.get("findings", []),
        report_urls={"json": f"/reports/{task_id}/report.json", "sarif": f"/reports/{task_id}/report.sarif" if "sarif_path" in report else None},
        completed_at=report.get("generated_at"),
    )


@app.get("/health")
async def health():
    try:
        r.ping()
        redis_ok = True
    except Exception:
        redis_ok = False
    return {"status": "ok" if redis_ok else "degraded", "redis": "connected" if redis_ok else "disconnected", "version": settings.API_VERSION}


@app.get("/")
async def root():
    return {"name": settings.API_TITLE, "version": settings.API_VERSION, "docs": "/docs", "health": "/health"}
```

---

## 五、环境配置

### 5.1 .env.example

```bash
# Redis
REDIS_URL=redis://localhost:6379/0

# API 认证
CODERISK_API_KEY=your-api-key-here

# CodeRisk Agent 路径
CODERISK_PATH=/path/to/code-risk-agent

# Nutrient DWS
NUTRIENT_DWS_API_KEY=your-nutrient-api-key

# 报告存储
REPORTS_DIR=./reports

# Worker 并发数
WORKER_CONCURRENCY=2
```

### 5.2 requirements.txt

```
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
celery[redis]>=5.4.0
redis>=5.0.0
pydantic>=2.0.0
httpx>=0.27.0
python-multipart>=0.0.9
```

### 5.3 启动命令

```bash
# 1. 启动 Redis
redis-server --daemonize yes

# 2. 启动 API Gateway
uvicorn app.main:app --host 0.0.0.0 --port 8000

# 3. 启动 Celery Worker
celery -A app.tasks worker --loglevel=info
```

---

## 六、测试验证

### 6.1 已通过的端到端测试

| 端点 | 测试方法 | 结果 |
|------|----------|------|
| `GET /health` | `curl localhost:8000/health` | ✅ `{"status": "ok", "redis": "connected"}` |
| `POST /api/v1/analyze` | 提交 direct_upload 任务 | ✅ 返回 task_id |
| `GET /api/v1/tasks/{id}` | 查询任务状态 | ✅ 4 Agent completed, progress 100% |
| `GET /api/v1/reports/{id}` | 获取报告 | ✅ findings + summary 返回 |

### 6.2 测试输出示例

```json
// POST /api/v1/analyze 响应
{
    "task_id": "cr-20260817-b7e5ec64",
    "status": "pending",
    "message": "Analysis task created. Use GET /api/v1/tasks/cr-20260817-b7e5ec64 to check progress."
}

// GET /api/v1/tasks/cr-20260817-b7e5ec64 响应
{
    "task_id": "cr-20260817-b7e5ec64",
    "status": "completed",
    "progress": 100,
    "agent_status": {
        "agent_1_static": "completed",
        "agent_2_semantic": "completed",
        "agent_3_verifier": "completed",
        "agent_4_report": "completed"
    },
    "updated_at": "2026-08-17T21:20:28.179387",
    "error": null
}
```

---

## 七、当前状态与待办

### 7.1 已完成（Day 1）

| 项目 | 状态 |
|------|------|
| FastAPI 骨架（5 个端点） | ✅ |
| Pydantic 数据模型 | ✅ |
| Celery 任务队列 | ✅ |
| Redis 集成 | ✅ |
| 4-Agent 调用链 | ✅（框架就绪，待接入真实 Agent） |
| SARIF 输出 | ✅ |
| API Key 认证 | ✅ |
| 端到端测试 | ✅ |

### 7.2 待完成

| 项目 | 计划 | 优先级 |
|------|------|--------|
| CodeRisk Agent 真实接入 | Day 7-8 | 🔴 P0 |
| Nutrient DWS 集成 | Day 9-10 | 🔴 P0 |
| GitHub Action | Day 15 | 🟡 P1 |
| Docker 化 | Day 13-14 | 🟡 P1 |
| 前端 Dashboard | Day 11-12 | 🟡 P1 |
| Bruno API 测试集 | Day 16 | 🟢 P2 |
| Demo 视频 | Day 17 | 🟢 P2 |

### 7.3 已知问题

1. **静态分析报错** — `_run_static_analysis` 传入的是目录路径字符串，但 CodeRisk Agent 的 `StaticAnalyzer.analyze()` 接口可能需要不同格式，需要适配
2. **Nutrient DWS 未集成** — PDF 生成 + 数字签名尚未实现
3. **direct_upload 模式未实现文件接收** — 目前只是创建空目录，需要实现 multipart 文件上传
4. **无前端** — Dashboard 尚未开发

---

## 八、架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        Client Layer                              │
│  GitHub Actions / IDE Plugin / Dashboard (React)                 │
└──────────────────────────────┬──────────────────────────────────┘
                               │ HTTP / Webhook
┌──────────────────────────────▼──────────────────────────────────┐
│                      Gateway Layer                               │
│  FastAPI + Uvicorn (CPU 环境)                                    │
│  POST /api/v1/analyze → 返回 task_id                             │
│  GET  /api/v1/tasks/{id} → 查询进度                              │
│  GET  /api/v1/reports/{id} → 获取报告                            │
└──────────────────────────────┬──────────────────────────────────┘
                               │ Celery Task
┌──────────────────────────────▼──────────────────────────────────┐
│                      Queue Layer                                 │
│  Redis (Celery Broker + Backend)                                 │
│  任务状态: pending → analyzing → verifying → completed           │
└──────────────────────────────┬──────────────────────────────────┘
                               │ Worker
┌──────────────────────────────▼──────────────────────────────────┐
│                      Worker Layer                                │
│  Celery Worker (AMD MI300X 192GB)                                │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐           │
│  │ Agent 1  │→│ Agent 2  │→│ Agent 3  │→│ Agent 4  │           │
│  │ Static   │ │ Semantic │ │ Verifier │ │ Report   │           │
│  │ (Semgrep)│ │ (LLM)   │ │ (Cross)  │ │ (Gen)    │           │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘           │
└──────────────────────────────┬──────────────────────────────────┘
                               │
┌──────────────────────────────▼──────────────────────────────────┐
│                    Document Layer                                │
│  Nutrient DWS (PDF Conversion + Digital Signature + Viewer)      │
│  JSON/SARIF → PDF → 签名 → 在线预览                              │
└─────────────────────────────────────────────────────────────────┘
```

---

## 九、与 CodeRisk Agent 的关系

CodeRisk Cloud **不修改** 原有 CodeRisk Agent 代码，而是通过 `sys.path.insert` 动态导入：

```python
sys.path.insert(0, settings.CODERISK_PATH)
from agents.static_analyzer import StaticAnalyzer
from agents.semantic_analyzer import SemanticAnalyzer
from agents.deep_verifier import DeepVerifier
```

CodeRisk Agent 保持独立，Cloud 版本只是包装层。
