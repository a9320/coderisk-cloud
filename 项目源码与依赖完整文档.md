# CodeRisk Cloud — 项目源码与依赖完整文档

**版本：** v1.0.0
**日期：** 2026-08-20
**仓库：** https://github.com/a9320/coderisk-cloud
**上游项目：** https://github.com/a9320/code-risk-agent（git submodule）

---

## 一、项目概述

CodeRisk Cloud 是 CodeRisk Agent 的 Cloud-Native API 演进版。

- **原项目：** 纯本地 CLI 工具，4-Agent AI 代码安全分析引擎，AMD GPU 本地推理
- **Cloud 版本：** FastAPI REST API + Celery 异步任务 + Redis 队列 + Streamlit Dashboard + Nutrient DWS PDF 报告

**一句话：** 把经过验证的本地安全引擎，封装成企业可用的 Cloud-Native API 服务。

---

## 二、目录结构

```
coderisk-cloud/
├── app/                          # FastAPI 应用（1,947 行 Python）
│   ├── __init__.py               # 空文件
│   ├── main.py          (387行)  # FastAPI 路由 + 8 个端点
│   ├── tasks.py         (443行)  # Celery 任务 + 4-Agent 调度
│   ├── models.py        (118行)  # Pydantic 数据模型
│   ├── config.py         (49行)  # 配置管理（环境变量）
│   ├── nutrient_client.py(429行) # Nutrient DWS PDF 生成 + 签名
│   ├── dashboard.py     (370行)  # Streamlit 可视化仪表板
│   └── demo_fixture.py  (150行)  # /demo 端点的预置示例数据
│
├── code-risk-agent/              # 原项目核心引擎（git submodule，4,996 行）
│   ├── main.py          (230行)  # CLI 入口
│   ├── orchestrator.py  (370行)  # 4-Agent 编排器
│   ├── agents/                   # 4 个 AI Agent
│   │   ├── static_analyzer.py    (472行) # Agent 1: 静态分析
│   │   ├── semantic_analyzer.py  (241行) # Agent 2: 语义分析（LLM）
│   │   ├── deep_verifier.py      (309行) # Agent 3: 深度验证
│   │   └── report_generator.py   (467行) # Agent 4: 报告生成
│   ├── core/                     # 核心工具库
│   │   ├── llm_client.py  (424行) # LLM 调用客户端（三层 fallback）
│   │   ├── models.py      (244行) # 数据模型
│   │   ├── attack_knowledge.py(226行) # 攻击模式知识库
│   │   ├── cve_client.py  (167行) # CVE 数据库查询
│   │   ├── dependency_scanner.py(373行) # 依赖扫描
│   │   ├── taint_analyzer.py(210行) # 污点分析
│   │   ├── semgrep_runner.py(136行) # Semgrep 集成
│   │   ├── memory.py      (251行) # Agent 记忆管理
│   │   └── retry.py        (59行) # 重试机制
│   ├── tests/                    # 51 个单元测试
│   ├── skill/                    # 天禧 Skill 适配版本
│   └── docs/                     # 项目文档
│
├── bruno/                        # Bruno API 测试集（9 个用例）
│   ├── environments/local.bru    # 环境变量（baseUrl, apiKey）
│   ├── health/health-check.bru   # 健康检查
│   ├── analyze/                  # 分析请求测试
│   │   ├── submit-github.bru     # GitHub 仓库分析
│   │   ├── submit-invalid.bru    # 无效请求
│   │   ├── missing-auth.bru      # 缺少认证
│   │   └── invalid-api-key.bru   # 错误 API Key
│   ├── tasks/get-task-status.bru # 任务状态查询
│   ├── tasks/not-found.bru       # 任务不存在
│   └── reports/get-report.bru    # 报告下载
│
├── tests/                        # Cloud 层测试
│   └── test_api.py               # API 集成测试
│
├── evidence/                     # 证据目录（评委验证用）
│   └── README.md
│
├── reports/                      # 生成的报告存储
│   ├── *.json                    # JSON 格式报告
│   ├── *.sarif                   # SARIF 格式报告
│   └── *.pdf                     # PDF 格式报告（Nutrient DWS）
│
├── backups/                      # 代码备份（旧版本）
│
├── 优化建议/                      # Kimi 的优化建议和代码交付
│
├── .github/workflows/            # CI/CD
│   ├── ci.yml                    # Lint + Test + Engine Tests
│   └── docker-build.yml          # Docker 构建 + 推送
│
├── Dockerfile                    # 统一镜像（API/Worker/Dashboard）
├── docker-compose.yml            # CPU 模式（4 服务）
├── docker-compose.gpu.yml        # GPU 模式（加 llama-server）
├── .dockerignore                 # Docker 构建排除
├── .gitmodules                   # Git submodule 配置
├── .gitignore                    # Git 忽略规则
├── .env.example                  # 环境变量模板
├── requirements.txt              # Python 依赖
├── verify.sh                     # 一键验证脚本（25 个检查）
├── JUDGES.md                     # 评委 3 分钟审查指南
├── README.md                     # 项目 README
├── DOCKER.md                     # Docker 部署文档
├── CodeRisk-Cloud-完整文档.md     # 完整技术文档（16,940 字）
├── 项目完整文档.md                # 另一份完整文档（29,166 字）
├── Day2-进展报告.md               # Day 2 进展
├── DevNetwork-作战计划.md         # 15 天作战计划
├── demo-script.md                # 英文 Demo 脚本
└── demo-script-cn.md             # 中文 Demo 脚本
```

---

## 三、Cloud 层源码详解（app/）

### 3.1 main.py — FastAPI 路由（387 行）

**8 个 API 端点：**

| 方法 | 路径 | 认证 | 功能 |
|------|------|------|------|
| GET | `/` | ❌ | 根路径，返回服务信息 |
| GET | `/health` | ❌ | 健康检查（Redis + Celery） |
| GET | `/demo` | ❌ | 预置示例扫描结果（9 条漏洞） |
| GET | `/docs` | ❌ | Swagger UI |
| POST | `/api/v1/analyze` | ✅ | 提交分析任务（GitHub/ZIP/Direct） |
| GET | `/api/v1/tasks/{task_id}` | ✅ | 查询任务状态和进度 |
| GET | `/api/v1/reports/{task_id}` | ✅ | 下载分析报告 |
| POST | `/api/v1/webhooks/github` | ❌ | GitHub Webhook（自动触发分析） |
| POST | `/api/v1/analyze/upload` | ✅ | ZIP 文件上传分析 |

**认证机制：**
```python
# Bearer Token 认证，防时序攻击
def verify_api_key(authorization: str = Header(None)):
    token = authorization.replace("Bearer ", "").strip()
    if not hmac.compare_digest(token, settings.API_KEY):
        raise HTTPException(status_code=403)
```

**默认 API Key：** `dev-key-change-in-production`（docker-compose.yml 中配置）

**GitHub Webhook：**
- 如果设置了 `GITHUB_WEBHOOK_SECRET`，验证 HMAC-SHA256 签名
- 如果未设置，跳过验证（开发模式）
- 支持 `push` 和 `pull_request` 事件

### 3.2 tasks.py — Celery 任务（443 行）

**Celery Worker 任务调度：**

```python
@celery_app.task(bind=True)
def run_analysis(self, task_id, source, source_config):
    # 阶段 1: Agent 1 — 静态分析
    # 阶段 2: Agent 2 — 语义分析（LLM）
    # 阶段 3: Agent 3 — 深度验证（去误报）
    # 阶段 4: Agent 4 — 报告生成（JSON/SARIF/PDF）
```

**4-Agent 引擎集成：**
- 通过 `sys.path.insert` 动态导入 `code-risk-agent` 的模块
- 调用 `orchestrator.py` 的 `Orchestrator` 类执行分析
- 结果存入 Redis + 写入 reports/ 目录

**ZIP 上传处理：**
- 解压 ZIP 文件到临时目录
- ZIP 炸弹防护（解压后大小限制）
- 文件数限制
- 路径遍历检测
- 分析完成后清理临时文件

**报告生成：**
- JSON 格式：直接写入文件
- SARIF 格式：从 JSON 转换
- PDF 格式：调用 Nutrient DWS API + SHA-256 数字签名

### 3.3 models.py — Pydantic 数据模型（118 行）

```python
class CodeSource(str, Enum):
    GITHUB = "github"
    ZIP = "zip"
    DIRECT_UPLOAD = "direct_upload"

class AnalyzeRequest(BaseModel):
    source: CodeSource
    repo_url: Optional[str]
    branch: str = "main"
    callback_url: Optional[str]
    output_formats: list[str] = ["json", "sarif"]

class TaskResponse(BaseModel):
    task_id: str
    status: TaskStatus  # pending/queued/analyzing/verifying/generating_report/completed/failed
    progress: int       # 0-100
    agent_status: AgentStatus

class ReportResponse(BaseModel):
    task_id: str
    summary: FindingSummary  # critical/high/medium/low/info 计数
    findings: list[dict]
    report_urls: ReportURLs
    digital_signature: Optional[str]
```

### 3.4 config.py — 配置管理（49 行）

**环境变量：**

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `REDIS_URL` | `redis://localhost:6379/0` | Redis 连接地址 |
| `CODERISK_PATH` | 自动探测 | code-risk-agent 路径 |
| `NUTRIENT_DWS_API_KEY` | 空 | Nutrient DWS API Key |
| `NUTRIENT_DWS_API_URL` | `https://api.nutrient.io/build` | Nutrient DWS 端点 |
| `CODERISK_API_KEY` | `dev-key-change-in-production` | API 认证 Key |
| `REPORTS_DIR` | `./reports` | 报告存储目录 |
| `GITHUB_WEBHOOK_SECRET` | 空 | Webhook 签名验证 |
| `WORKER_CONCURRENCY` | `2` | Worker 并发数 |

**路径自动探测优先级：**
1. 环境变量 `CODERISK_PATH`
2. `../code-risk-agent`（开发环境）
3. `/app/code-risk-agent`（Docker 环境）
4. `~/code-risk-agent`

### 3.5 nutrient_client.py — Nutrient DWS 集成（429 行）

**功能：**
- HTML 报告模板（5 段式：概要→发现→详情→建议→附录）
- 调用 Nutrient DWS API 生成 PDF（POST https://api.nutrient.io/build）
- SHA-256 数字签名（本地计算）
- 优雅降级：Key 为空时跳过 PDF 生成

**调用流程：**
```python
html_report = build_html_template(findings)  # 生成 HTML
pdf_bytes = await client.generate_pdf(html_report)  # Nutrient API → PDF
signed_bytes = await client.sign_pdf(pdf_bytes)  # SHA-256 签名
```

### 3.6 dashboard.py — Streamlit 仪表板（370 行）

**功能：**
- 提交 GitHub 仓库 URL 或上传 ZIP 文件
- 实时显示分析进度（轮询任务状态）
- 展示漏洞统计（饼图/柱状图）
- 下载报告（JSON/SARIF/PDF）
- 历史任务列表

**访问地址：** http://localhost:8501

### 3.7 demo_fixture.py — 预置示例数据（150 行）

**9 条示例漏洞：**

| ID | 严重等级 | 类型 | 文件 |
|----|---------|------|------|
| CR-001 | Critical | SQL Injection | app/routes/users.py:42 |
| CR-002 | High | Command Injection | app/utils/network.py:18 |
| CR-003 | High | Hardcoded Secret | config.py:7 |
| CR-004 | Medium | XSS | app/templates/profile.html:12 |
| CR-005 | Medium | Insecure Deserialization | app/utils/cache.py:31 |
| CR-006 | Medium | Path Traversal | app/routes/files.py:25 |
| CR-007 | Low | Missing CSRF Protection | app/routes/auth.py:55 |
| CR-008 | Low | Verbose Error Messages | app/__init__.py:15 |
| CR-009 | Info | Dependency Outdated | requirements.txt:3 |

---

## 四、核心引擎详解（code-risk-agent/）

### 4.1 架构：4-Agent 流水线

```
输入代码 → Agent 1 → Agent 2 → Agent 3 → Agent 4 → 报告
            静态分析    语义分析    深度验证    报告生成
           (472行)     (241行)     (309行)     (467行)
```

### 4.2 Agent 1 — Static Analyzer（472 行）

- AST 解析 Python/C/JavaScript 代码
- 正则模式匹配（SQL 注入、命令注入、XSS 等）
- Semgrep 规则集成
- 污点分析（数据流追踪）
- 输出：初步漏洞列表 + 置信度分数

### 4.3 Agent 2 — Semantic Analyzer（241 行）

- 调用 LLM 理解代码语义
- 上下文分析（函数调用链、数据流向）
- 识别 Agent 1 可能遗漏的逻辑漏洞
- 攻击知识库匹配（226 行攻击模式库）
- 输出：补充漏洞列表 + 语义分析报告

### 4.4 Agent 3 — Deep Verifier（309 行）

- 交叉验证 Agent 1 和 Agent 2 的结果
- 误报过滤（上下文检查、可达性分析）
- CVE 数据库查询（已知漏洞匹配）
- 依赖扫描（npm/pip/go 等）
- 输出：确认漏洞列表（去误报后）

### 4.5 Agent 4 — Report Generator（467 行）

- JSON 格式报告
- SARIF 2.1.0 格式（静态分析结果交换格式）
- Markdown 格式报告
- 修复建议生成（LLM 辅助）
- 输出：多格式报告文件

### 4.6 Orchestrator（370 行）

- 编排 4 个 Agent 的执行顺序
- 结果聚合和去重
- 错误处理和重试机制
- 进度回调

### 4.7 Core 工具库

| 模块 | 行数 | 功能 |
|------|------|------|
| llm_client.py | 424 | 三层 fallback：用户 Key → 平台 Token → 规则兜底 |
| models.py | 244 | 数据模型（Finding/Vulnerability/Report） |
| attack_knowledge.py | 226 | 攻击模式知识库（SQLi/XSS/RCE/SSRF 等） |
| cve_client.py | 167 | CVE 数据库查询（NVD/OSV） |
| dependency_scanner.py | 373 | 依赖扫描（npm/pip/go/gem） |
| taint_analyzer.py | 210 | 污点分析（数据流追踪） |
| semgrep_runner.py | 136 | Semgrep 集成 |
| memory.py | 251 | Agent 记忆管理（上下文窗口） |
| retry.py | 59 | 指数退避重试 |

---

## 五、依赖清单

### 5.1 Python 直接依赖（requirements.txt）

| 包 | 版本 | 用途 |
|----|------|------|
| fastapi | >=0.115.0 | Web 框架 |
| uvicorn[standard] | >=0.34.0 | ASGI 服务器 |
| celery[redis] | >=5.4.0 | 异步任务队列 |
| redis | >=5.0.0 | Redis 客户端 |
| pydantic | >=2.0.0 | 数据验证 |
| httpx | >=0.27.0 | HTTP 客户端 |
| python-multipart | >=0.0.9 | 文件上传 |
| streamlit | >=1.30.0 | 可视化仪表板 |

### 5.2 间接依赖（估算 40-50 个包）

**主要间接依赖：**
- starlette（ASGI 框架，fastapi 依赖）
- click（CLI 工具，uvicorn 依赖）
- kombu/vine/amqp（消息队列，celery 依赖）
- numpy/pandas（数据处理，streamlit 依赖）
- pyarrow（数据格式，streamlit 依赖）
- altair（可视化，streamlit 依赖）
- httpcore/certifi/idna（HTTP 基础，httpx 依赖）
- pydantic-core/annotated-types（pydantic 依赖）

### 5.3 系统依赖（Dockerfile）

| 包 | 用途 |
|----|------|
| git | clone 子模块 |
| gcc / build-base / musl-dev | 编译 Python C 扩展 |
| ca-certificates | HTTPS 证书 |

### 5.4 code-risk-agent 额外依赖

详见 `code-risk-agent/pyproject.toml`，主要包括：
- semgrep（静态分析引擎）
- tree-sitter（AST 解析）
- aiohttp（异步 HTTP）

---

## 六、Docker 部署

### 6.1 镜像结构

**统一镜像** — 一个 Dockerfile，三种运行模式（通过 docker-compose 覆盖 command）：

| 模式 | 命令 | 端口 |
|------|------|------|
| API | `uvicorn app.main:app` | 8000 |
| Worker | `celery -A app.tasks worker` | 无 |
| Dashboard | `streamlit run app/dashboard.py` | 8501 |

### 6.2 docker-compose.yml（CPU 模式）

**4 个服务：**

| 服务 | 镜像 | 端口 | 说明 |
|------|------|------|------|
| redis | redis:7-alpine | 6379 | 消息队列 + 状态存储 |
| api | 自建 | 8000 | FastAPI 网关 |
| worker | 自建 | 无 | Celery 任务执行 |
| dashboard | 自建 | 8501 | Streamlit 仪表板 |

**依赖关系：**
```
api → redis (service_healthy)
worker → redis + api
dashboard → api
```

### 6.3 docker-compose.gpu.yml（GPU 模式）

额外服务：
| 服务 | 镜像 | 端口 | 说明 |
|------|------|------|------|
| llama-server | ghcr.io/ggml-org/llama.cpp:server | 8080 | 本地 LLM 推理 |

GPU 配置：
```yaml
deploy:
  resources:
    reservations:
      devices:
        - driver: nvidia
          count: 1
          capabilities: [gpu]
```

### 6.4 环境变量

| 变量 | docker-compose 默认值 | 说明 |
|------|---------------------|------|
| REDIS_URL | redis://redis:6379/0 | Redis 连接 |
| CODERISK_API_KEY | ${CODERISK_API_KEY:-dev-ke…} | API 认证 |
| CODERISK_PATH | /app/code-risk-agent | 引擎路径 |
| NUTRIENT_DWS_API_KEY | ${NUTRIENT_DWS_API_KEY:-} | Nutrient Key |
| REPORTS_DIR | /app/reports | 报告目录 |
| WORKER_CONCURRENCY | ${WORKER_CONCURRENCY:-2} | Worker 并发 |
| LOCAL_HTTP_URL | http://host.docker.internal:8080 | LLM 服务地址 |

---

## 七、CI/CD

### 7.1 ci.yml — 持续集成

**触发条件：** push to main/dev, PR to main

**步骤：**
1. Checkout（含 submodule）
2. Python 3.12 环境
3. pip 缓存
4. 安装依赖 + pytest + safety + pip-audit
5. 引擎单元测试（code-risk-agent/tests/）
6. Cloud 层测试（tests/）
7. 安全扫描（pip-audit）
8. 代码覆盖率报告

### 7.2 docker-build.yml — Docker 构建

**触发条件：** push to main, tag push

**步骤：**
1. Checkout
2. Docker Buildx
3. 构建 + 推送镜像到 GHCR

---

## 八、验证体系

### 8.1 verify.sh（25 个检查项）

**检查类别：**
- 服务可用性（health/demo/docs 端点）
- API 认证（无 key 拒绝、错误 key 拒绝）
- 任务流程（提交→状态→报告）
- 报告格式（JSON 结构验证）
- Docker 容器状态
- 安全检查

### 8.2 Bruno 测试集（9 个用例）

| # | 用例 | 方法 | 端点 | 期望 |
|---|------|------|------|------|
| 1 | 健康检查 | GET | /health | 200 |
| 2 | GitHub 分析 | POST | /api/v1/analyze | 200 + task_id |
| 3 | ZIP 上传 | POST | /api/v1/analyze/upload | 200 + task_id |
| 4 | 无认证拒绝 | POST | /api/v1/analyze | 401 |
| 5 | 错误 Key 拒绝 | POST | /api/v1/analyze | 403 |
| 6 | 任务状态 | GET | /api/v1/tasks/{id} | 200 |
| 7 | 任务不存在 | GET | /api/v1/tasks/fake | 404 |
| 8 | 报告下载 | GET | /api/v1/reports/{id} | 200 |
| 9 | Demo 端点 | GET | /demo | 200 + 9 findings |

---

## 九、已知限制

1. **LLM 依赖：** Agent 2（语义分析）需要 LLM 服务，无 LLM 时降级为纯规则模式
2. **语言支持：** 主要支持 Python，C/JavaScript 次之，其他语言覆盖有限
3. **PDF 生成：** 依赖 Nutrient DWS 外部服务，Key 为空时跳过
4. **并发限制：** 默认 Worker 并发数为 2，可通过环境变量调整
5. **ZIP 上传：** 限制 100MB，解压后 500MB
6. **GitHub 仓库：** 仅支持公开仓库（无私有仓库认证）

---

## 十、关键文件路径速查

| 文件 | 用途 |
|------|------|
| `app/main.py` | API 路由定义 |
| `app/tasks.py` | Celery 任务 + Agent 调度 |
| `app/config.py` | 环境变量配置 |
| `app/models.py` | 数据模型 |
| `app/nutrient_client.py` | PDF 生成 |
| `app/dashboard.py` | Streamlit 仪表板 |
| `app/demo_fixture.py` | 示例数据 |
| `code-risk-agent/orchestrator.py` | 4-Agent 编排 |
| `code-risk-agent/agents/*.py` | 4 个 AI Agent |
| `code-risk-agent/core/llm_client.py` | LLM 调用 |
| `docker-compose.yml` | Docker 部署配置 |
| `Dockerfile` | 镜像构建 |
| `verify.sh` | 验证脚本 |
| `JUDGES.md` | 评委指南 |
| `.github/workflows/ci.yml` | CI 配置 |
| `requirements.txt` | Python 依赖 |
| `.env.example` | 环境变量模板 |
