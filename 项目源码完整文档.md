# CodeRisk Cloud — 项目源码完整文档

> **项目名称：** CodeRisk Cloud  
> **版本：** 1.0.0  
> **团队：** AI溢出安全实验室 (Overflow Security Lab)  
> **赛事：** DevNetwork 2026  
> **生成时间：** 2026-08-24

---

## 目录

- [一、项目概述](#一项目概述)
- [二、系统架构](#二系统架构)
- [三、API 层（app/）](#三api-层app)
  - [3.1 app/main.py — FastAPI 主应用](#31-appmainpy--fastapi-主应用)
  - [3.2 app/config.py — 配置管理](#32-appconfigpy--配置管理)
  - [3.3 app/models.py — Pydantic 数据模型](#33-appmodelspy--pydantic-数据模型)
  - [3.4 app/tasks.py — Celery 异步任务](#34-apptaskspy--celery-异步任务)
  - [3.5 app/dashboard.py — Streamlit 可视化仪表盘](#35-appdashboardpy--streamlit-可视化仪表盘)
  - [3.6 app/demo_fixture.py — Demo 数据](#36-appdemo_fixturepy--demo-数据)
  - [3.7 app/nutrient_client.py — Nutrient PDF 客户端](#37-appnutrient_clientpy--nutrient-pdf-客户端)
- [四、核心引擎（code-risk-agent/）](#四核心引擎code-risk-agent)
  - [4.1 core/models.py — 核心数据模型](#41-coremodelspy--核心数据模型)
  - [4.2 core/llm_client.py — LLM 客户端](#42-corellm_clientpy--llm-客户端)
  - [4.3 core/taint_analyzer.py — 污点分析器](#43-coretaint_analyzerpy--污点分析器)
  - [4.4 core/dependency_scanner.py — 依赖扫描器](#44-coredependency_scannerpy--依赖扫描器)
  - [4.5 core/cve_client.py — CVE 数据库客户端](#45-corecve_clientpy--cve-数据库客户端)
  - [4.6 core/memory.py — 记忆层](#46-corememorypy--记忆层)
  - [4.7 core/semgrep_runner.py — Semgrep 运行器](#47-coresemgrep_runnerpy--semgrep-运行器)
  - [4.8 core/attack_knowledge.py — ATT&CK 知识库](#48-coreattack_knowledgepy--attck-知识库)
  - [4.9 core/retry.py — 重试策略](#49-coreretryptpy--重试策略)
  - [4.10 agents/static_analyzer.py — Agent 1 静态分析器](#410-agentsstatic_analyzerpy--agent-1-静态分析器)
  - [4.11 agents/semantic_analyzer.py — Agent 2 语义分析器](#411-agentssemantic_analyzerpy--agent-2-语义分析器)
  - [4.12 agents/deep_verifier.py — Agent 3 深度验证器](#412-agentsdeep_verifierpy--agent-3-深度验证器)
  - [4.13 agents/report_generator.py — Agent 4 报告生成器](#413-agentsreport_generatorpy--agent-4-报告生成器)
  - [4.14 orchestrator.py — 编排器](#414-orchestratorpy--编排器)
  - [4.15 main.py — CLI 入口](#415-mainpy--cli-入口)
- [五、配置与部署](#五配置与部署)
  - [5.1 Dockerfile](#51-dockerfile)
  - [5.2 docker-compose.yml](#52-docker-composeyml)
  - [5.3 docker-compose.gpu.yml](#53-docker-composegpuyml)
  - [5.4 requirements.txt](#54-requirementstxt)
  - [5.5 .env.example](#55-envexample)
  - [5.6 .gitmodules](#56-gitmodules)
- [六、CI/CD](#六cicd)
  - [6.1 .github/workflows/ci.yml](#61-githubworkflowsci_yml)
  - [6.2 .github/workflows/docker-build.yml](#62-githubworkflowsdocker-build_yml)
- [七、测试](#七测试)
  - [7.1 tests/conftest.py](#71-testsconftestpy)
  - [7.2 tests/test_health.py](#72-teststest_healthpy)
  - [7.3 tests/test_demo.py](#73-teststest_demopy)
  - [7.4 tests/test_auth.py](#74-teststest_authpy)
  - [7.5 verify.sh](#75-verifysh)
- [八、API 端点汇总](#八api-端点汇总)
- [九、数据模型汇总](#九数据模型汇总)
- [十、配置项汇总](#十配置项汇总)
- [十一、模块关系图](#十一模块关系图)
- [十二、已知问题](#十二已知问题)

---

## 一、项目概述

CodeRisk Cloud 是一个 **AI 驱动的代码安全分析平台**，采用多 Agent 架构，集成了静态分析、语义分析（LLM）、深度验证和报告生成四大能力。

### 核心特性

- **四阶段分析流水线：** 静态分析 → 语义分析 → 深度验证 → 报告生成
- **多源代码输入：** GitHub 仓库、ZIP 上传、本地目录扫描
- **多种分析引擎：** Tree-sitter 模式匹配 + Semgrep + 污点分析 + 依赖扫描 + LLM 语义分析
- **记忆系统：** 正确记忆 + 错误记忆，支持模式学习和误报抑制
- **报告格式：** JSON / SARIF / PDF（Nutrient DWS 生成）
- **本地 GPU 推理：** 支持 AMD ROCm + llama-cpp-python，无需云端 API

### 技术栈

| 层级 | 技术 |
|------|------|
| API 框架 | FastAPI + Uvicorn |
| 异步任务 | Celery + Redis |
| 前端仪表盘 | Streamlit |
| LLM 推理 | llama-cpp-python / llama-server (Qwen2.5-Coder) |
| 静态分析 | Tree-sitter + Semgrep + 正则模式匹配 |
| PDF 生成 | Nutrient DWS API |
| 容器化 | Docker + Docker Compose |
| CI/CD | GitHub Actions |

---

## 二、系统架构

### 整体模块关系

```
┌─────────────────────────────────────────────────────────────────┐
│                        客户端层                                  │
│  ┌──────────┐  ┌──────────────┐  ┌────────────┐                │
│  │ Swagger  │  │  Streamlit   │  │  GitHub    │                │
│  │  /docs   │  │  Dashboard   │  │  Webhook   │                │
│  └────┬─────┘  └──────┬───────┘  └─────┬──────┘                │
└───────┼────────────────┼───────────────┼────────────────────────┘
        │                │               │
┌───────┼────────────────┼───────────────┼────────────────────────┐
│       ▼                ▼               ▼       API 层           │
│  ┌─────────────────────────────────────────┐                    │
│  │           app/main.py (FastAPI)         │                    │
│  │  /api/v1/analyze  /tasks  /reports      │                    │
│  │  /api/v1/analyze/upload  /scan-local    │                    │
│  │  /api/v1/webhooks/github  /health /demo │                    │
│  └─────────────────┬───────────────────────┘                    │
│                    │                                             │
│  ┌─────────────────▼───────────────────────┐                    │
│  │        app/tasks.py (Celery)            │                    │
│  │  analyze_codebase_task                  │                    │
│  └─────────────────┬───────────────────────┘                    │
│                    │                                             │
│  ┌─────────────┐  │  ┌──────────────────┐  ┌─────────────────┐ │
│  │ app/config  │  │  │ app/nutrient_    │  │ app/models      │ │
│  │ Settings    │  │  │ client PDF生成   │  │ Pydantic schemas│ │
│  └─────────────┘  │  └──────────────────┘  └─────────────────┘ │
└───────────────────┼─────────────────────────────────────────────┘
                    │
┌───────────────────┼─────────────────────────────────────────────┐
│                   ▼         核心引擎 (code-risk-agent/)          │
│  ┌─────────────────────────────────────────┐                    │
│  │         orchestrator.py                 │                    │
│  │    状态机: INIT→PARSE→ANALYZE→VERIFY    │                    │
│  │              →REPORT→DONE               │                    │
│  └───┬────────┬────────┬────────┬──────────┘                    │
│      │        │        │        │                                │
│      ▼        ▼        ▼        ▼                                │
│  ┌───────┐┌───────┐┌───────┐┌───────┐                          │
│  │Agent 1││Agent 2││Agent 3││Agent 4│                          │
│  │静态   ││语义   ││深度   ││报告   │                          │
│  │分析器 ││分析器 ││验证器 ││生成器 │                          │
│  └───┬───┘└───┬───┘└───┬───┘└───┬───┘                          │
│      │        │        │        │                                │
│      ▼        ▼        ▼        ▼                                │
│  ┌──────────────────────────────────────┐                       │
│  │           core/ 核心模块              │                       │
│  │ models │ llm_client │ taint_analyzer │                       │
│  │ dependency_scanner │ cve_client      │                       │
│  │ memory │ semgrep_runner              │                       │
│  │ attack_knowledge │ retry             │                       │
│  └──────────────────────────────────────┘                       │
└─────────────────────────────────────────────────────────────────┘
```

### 四阶段分析流水线

```
代码输入 (GitHub/ZIP/Local)
    │
    ▼
┌─────────────────────────────────────────────┐
│ 阶段 1: 静态分析 (Agent 1 + 并行扩展)       │
│  • Agent 1: Tree-sitter 正则模式匹配         │
│  • Agent 1b: 污点分析 (source→sink 追踪)     │
│  • Agent 1c: 依赖扫描 (OSV/CVE 已知漏洞)     │
│  • Semgrep: 外部规则引擎                      │
│  ↓ 输出: 初始风险列表                        │
└─────────────────────┬───────────────────────┘
                      │ (ThreadPoolExecutor 并行)
                      ▼
┌─────────────────────────────────────────────┐
│ 阶段 2: 语义分析 (Agent 2, LLM)             │
│  • 验证 Agent 1 的发现 (TRUE/FALSE positive) │
│  • 发现 Agent 1 遗漏的新漏洞                  │
│  • 攻击场景分析 + 影响评估                    │
│  ↓ 输出: 增强后的风险列表                    │
└─────────────────────┬───────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────┐
│ 阶段 3: 深度验证 (Agent 3)                   │
│  • 工具交叉验证 (Semgrep + Pattern + LLM)    │
│  • 知识库交叉验证 (CVE/NVD 查询)              │
│  • 记忆验证 (已知误报抑制 + 已知真报增强)      │
│  • 自反思循环 (迭代发现遗漏, 最多 2 轮)       │
│  ↓ 输出: 验证后的风险列表                    │
└─────────────────────┬───────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────┐
│ 阶段 4: 报告生成 (Agent 4)                   │
│  • JSON 结构化报告                            │
│  • SARIF 2.1.0 标准报告                      │
│  • PDF 专业报告 (Nutrient DWS)               │
│  • SHA-256 数字签名                           │
│  ↓ 输出: 多格式安全审计报告                  │
└─────────────────────────────────────────────┘
```

---

## 三、API 层（app/）

### 3.1 app/main.py — FastAPI 主应用

**文件路径：** `app/main.py`  
**用途：** CodeRisk Cloud 的 API 入口，定义所有 HTTP 端点  
**行数：** ~280 行

#### 核心逻辑

1. **FastAPI 应用初始化** — 配置 CORS、日志、Redis 连接
2. **API Key 认证** — `verify_api_key()` 使用 `hmac.compare_digest` 防时序攻击
3. **任务管理** — 任务创建、状态查询、报告获取，全部通过 Redis 存储
4. **GitHub Webhook** — 接收 Push 事件自动触发分析
5. **健康检查** — 检测 Redis、Celery Worker、GPU 状态
6. **文件上传** — ZIP 文件上传分析，含大小限制和格式验证
7. **本地扫描** — `/api/v1/scan-local` 扫描 Docker 容器内目录

#### 关键函数

| 函数 | 路由 | 方法 | 用途 |
|------|------|------|------|
| `analyze()` | `/api/v1/analyze` | POST | 提交 GitHub 仓库分析任务 |
| `analyze_upload()` | `/api/v1/analyze/upload` | POST | 上传 ZIP 文件分析 |
| `get_task_status()` | `/api/v1/tasks/{task_id}` | GET | 查询任务状态和进度 |
| `get_report()` | `/api/v1/reports/{task_id}` | GET | 获取分析报告 |
| `download_report_pdf()` | `/api/v1/reports/{task_id}/pdf` | GET | 下载 PDF 报告 |
| `github_webhook()` | `/api/v1/webhooks/github` | POST | GitHub Webhook 接收 |
| `scan_local()` | `/api/v1/scan-local` | POST | 本地目录扫描 |
| `health()` | `/health` | GET | 健康检查 |
| `demo()` | `/demo` | GET | Demo 数据（无需认证） |
| `root()` | `/` | GET | 根路由信息 |

#### 关键代码片段

```python
# API Key 防时序攻击验证
def verify_api_key(authorization: str | None = Header(None)):
    if not authorization:
        raise HTTPException(status_code=401, detail="Missing Authorization header")
    token = authorization.replace("Bearer ", "").strip()
    if not hmac.compare_digest(token, settings.API_KEY):
        raise HTTPException(status_code=403, detail="Invalid API Key")
    return token

# 报告隔离校验（防止跨用户访问）
current_key_hash = hashlib.sha256(authorization.encode()).hexdigest()[:16]
if meta.get("api_key_hash") != current_key_hash:
    raise HTTPException(status_code=403, detail="Access denied for this report")
```

#### 与其他文件的关系

- **依赖：** `app/config.py`（配置）、`app/models.py`（数据模型）、`app/tasks.py`（Celery 任务）、`app/demo_fixture.py`（Demo 数据）
- **被依赖：** Dockerfile CMD 启动此文件、`tests/` 中的测试通过 TestClient 调用

---

### 3.2 app/config.py — 配置管理

**文件路径：** `app/config.py`  
**用途：** 集中管理所有配置项，从环境变量读取  
**行数：** ~40 行

#### 核心逻辑

`Settings` 类从环境变量读取配置，提供默认值。`CODERISK_PATH` 支持多路径自动探测。

#### 关键配置项

| 配置项 | 环境变量 | 默认值 | 说明 |
|--------|----------|--------|------|
| `REDIS_URL` | `REDIS_URL` | `redis://localhost:6379/0` | Redis 连接 URL |
| `CODERISK_PATH` | `CODERISK_PATH` | 自动探测 | code-risk-agent 路径 |
| `NUTRIENT_API_KEY` | `NUTRIENT_DWS_API_KEY` | `""` | Nutrient PDF API Key |
| `API_KEY` | `CODERISK_API_KEY` | `dev-key-change-in-production` | API 认证密钥 |
| `REPORTS_DIR` | `REPORTS_DIR` | `./reports` | 报告存储目录 |
| `GITHUB_WEBHOOK_SECRET` | `GITHUB_WEBHOOK_SECRET` | `""` | GitHub Webhook 签名密钥 |
| `WORKER_CONCURRENCY` | `WORKER_CONCURRENCY` | `2` | Celery Worker 并发数 |

#### 关键代码片段

```python
# CODERISK_PATH 自动探测逻辑
_default_paths = [
    os.getenv("CODERISK_PATH"),
    str(Path(__file__).parent.parent.parent / "code-risk-agent"),
    "/app/code-risk-agent",
    str(Path.home() / "code-risk-agent"),
]
CODERISK_PATH = next(
    (p for p in _default_paths if p and Path(p).exists()),
    None
)
```

#### 与其他文件的关系

- **被依赖：** `app/main.py`、`app/tasks.py`、`app/nutrient_client.py` 全部导入 `settings`

---

### 3.3 app/models.py — Pydantic 数据模型

**文件路径：** `app/models.py`  
**用途：** 定义 API 请求/响应的 Pydantic 数据模型  
**行数：** ~120 行

#### 核心模型

| 模型 | 类型 | 用途 |
|------|------|------|
| `CodeSource` | Enum | 代码来源：`github` / `zip` / `direct_upload` |
| `AnalyzeRequest` | 请求 | 分析请求（source, repo_url, branch, output_formats） |
| `TaskStatus` | Enum | 任务状态：pending → queued → analyzing → verifying → generating_report → completed / failed |
| `AgentStatus` | 响应 | 四个 Agent 的执行状态 |
| `TaskResponse` | 响应 | 任务状态查询响应 |
| `FindingSummary` | 响应 | 漏洞摘要（critical/high/medium/low/info 计数） |
| `ReportURLs` | 响应 | 报告下载链接 |
| `ReportResponse` | 响应 | 完整报告响应 |
| `AnalyzeResponse` | 响应 | 分析请求的初始响应 |
| `ErrorResponse` | 响应 | 错误响应 |

#### 关键代码片段

```python
class TaskStatus(str, Enum):
    PENDING = "pending"
    QUEUED = "queued"
    ANALYZING = "analyzing"
    VERIFYING = "verifying"
    GENERATING_REPORT = "generating_report"
    COMPLETED = "completed"
    FAILED = "failed"
```

#### 与其他文件的关系

- **被依赖：** `app/main.py` 导入所有请求/响应模型用于路由类型注解
- **独立于：** `code-risk-agent/core/models.py`（这是 API 层的模型，引擎有自己独立的模型）

---

### 3.4 app/tasks.py — Celery 异步任务

**文件路径：** `app/tasks.py`  
**用途：** 定义 Celery 异步任务，实现完整的四阶段分析流水线  
**行数：** ~500 行  
**这是整个项目的核心编排文件，连接 API 层和引擎层。**

> **Kimi v33 集成变更：** 新增 `_scan_code_files`、`_dict_to_risk`、`_risk_to_dict` 三个 Cloud↔引擎数据转换函数；`_run_static_analysis`、`_run_semantic_analysis`、`_run_verification` 改用新转换函数；`_generate_report` 增加 Celery 事件循环兼容；保留 `_run_taint_analysis` 和 `_run_dependency_scan`（Kimi v33 曾删除，lolo 补回）；删除旧版 `_risks_to_dicts`。

#### 核心逻辑

1. **Celery 配置：** Redis 作为 Broker 和 Backend，JSON 序列化，30 分钟硬限制
2. **引擎模块懒加载：** `_get_static_analyzer()`、`_get_semantic_analyzer()`、`_get_deep_verifier()` 延迟初始化
3. **Cloud↔引擎数据转换（Kimi v33 集成）：**
   - `_scan_code_files()` — 扫描工作目录，收集 `.c`/`.h`/`.py` 文件为 `CodeFile` 对象，含 `ImportError` 处理和日志
   - `_dict_to_risk()` — API 层 dict → 引擎层 `Risk` 对象转换（含 severity/confidence/language 健壮映射）
   - `_risk_to_dict()` — 引擎层 `Risk` → API 层 dict 转换（confidence 枚举→百分比）
4. **四阶段分析流水线：**
   - 阶段 1：准备代码（Git clone / ZIP 解压 / 本地路径）
   - 阶段 2：Agent 1（静态）+ Agent 2（语义）并行执行 + 污点分析 + 依赖扫描
   - 阶段 3：Agent 3 深度验证
   - 阶段 4：Agent 4 报告生成（JSON/SARIF/PDF）
5. **进度更新：** 通过 Redis 实时更新任务状态
6. **资源清理：** finally 块清理临时目录和上传的 ZIP 文件

#### 关键函数

| 函数 | 用途 | Kimi v33 变更 |
|------|------|---------------|
| `analyze_codebase_task()` | Celery 主任务，编排四阶段流水线 | 无变更 |
| `_prepare_code()` | 准备代码（clone/解压/本地路径），含安全校验 | 无变更 |
| `_scan_code_files()` | 扫描目录收集 .c/.h/.py 文件为 CodeFile 对象 | ✅ **新增**（替代旧版，增加 ImportError 处理和日志） |
| `_dict_to_risk()` | API 层 dict → 引擎层 Risk 对象转换 | ✅ **新增** |
| `_risk_to_dict()` | 引擎层 Risk → API 层 dict 转换 | ✅ **新增**（替代 `_risks_to_dicts`） |
| `_run_static_analysis()` | Agent 1 静态分析 | 🔄 改用 `_scan_code_files` + `_risk_to_dict` |
| `_run_semantic_analysis()` | Agent 2 LLM 语义分析 | 🔄 改用 `_scan_code_files` + `_risk_to_dict` |
| `_run_taint_analysis()` | Agent 1b 污点分析 | 🔒 **保留**（Kimi v33 删除，lolo 补回） |
| `_run_dependency_scan()` | Agent 1c 依赖扫描 | 🔒 **保留**（Kimi v33 删除，lolo 补回） |
| `_run_verification()` | Agent 3 深度验证 | 🔄 改用 `_dict_to_risk` + `_risk_to_dict` |
| `_generate_report()` | Agent 4 报告生成 | 🔄 增加 RuntimeError 事件循环冲突捕获 |
| `_merge_findings()` | 合并去重（按 severity 取更高） | 无变更 |
| `_to_sarif()` | 转换为 SARIF 2.1.0 格式 | 无变更 |
| `_update_progress()` | 更新 Redis 中的任务进度 | 无变更 |

> **已删除函数：** `_risks_to_dicts` — 被 `_risk_to_dict` 替代（逐个转换而非批量）

#### Cloud↔引擎数据转换（Kimi v33 核心变更）

```python
# _scan_code_files — 扫描工作目录，收集 CodeFile 对象
def _scan_code_files(work_dir: str) -> list:
    try:
        from core.models import CodeFile
    except ImportError as e:
        logger.error(f"Cannot import CodeFile from engine: {e}")
        return []
    code_files = []
    for ext in (".c", ".h", ".py"):
        for file_path in Path(work_dir).rglob(f"*{ext}"):
            if file_path.is_file():
                code_files.append(CodeFile.from_path(file_path))
    return code_files

# _dict_to_risk — API 层 dict → 引擎层 Risk 对象（健壮映射）
def _dict_to_risk(finding: dict):
    # severity: 字符串 → Severity 枚举（未知值降级为 INFO）
    # confidence: 数值百分比 → Confidence 枚举（HIGH≥70 / MEDIUM≥40 / LOW<40）
    # language: 字符串 → Language 枚举（未知值降级为 UNKNOWN）
    # evidence: 从 code_snippet 构建 Evidence 对象
    return Risk(id=..., title=..., severity=..., confidence=..., ...)

# _risk_to_dict — 引擎层 Risk → API 层 dict
def _risk_to_dict(risk) -> dict:
    # confidence 枚举 → 百分比: {"high": 80, "medium": 50, "low": 30}
    return {"id": risk.id, "type": risk.cwe_id, "severity": sev, "confidence": conf_pct, ...}
```

#### 关键代码片段

```python
# 阶段 2: Agent 1 + Agent 2 并行执行
with ThreadPoolExecutor(max_workers=2) as executor:
    future_static = executor.submit(_run_static_analysis, task_id, work_dir)
    future_semantic = executor.submit(_run_semantic_analysis, task_id, work_dir, [])
    static_findings = future_static.result()
    semantic_findings = future_semantic.result()

# ZIP 炸弹防护
total_size = sum(f.file_size for f in infolist)
MAX_DECOMPRESSED = 100 * 1024 * 1024  # 100 MB
if total_size > MAX_DECOMPRESSED:
    raise ValueError(f"ZIP decompressed size exceeds limit")

# 路径遍历防护
for member in infolist:
    target = work_dir / member.filename
    target.resolve().relative_to(work_dir.resolve())  # 如果不在 work_dir 内会抛异常

# _generate_report — Celery 事件循环兼容（Kimi v33 新增）
except RuntimeError as e:
    if "cannot be called from a running event loop" in str(e):
        logger.warning(f"PDF generation skipped: event loop conflict in Celery worker")
        report["pdf_path"] = None
        report["pdf_error"] = "PDF generation not supported in async worker context"
    else:
        raise
```

#### 与其他文件的关系

- **依赖：** `app/config.py`、`app/nutrient_client.py`、`code-risk-agent/` 中的所有引擎模块（`core.models.CodeFile`、`core.models.Risk`、`core.taint_analyzer`、`core.dependency_scanner`）
- **被依赖：** `app/main.py` 调用 `analyze_codebase_task.delay()` 触发异步任务

---

### 3.5 app/dashboard.py — Streamlit 可视化仪表盘

**文件路径：** `app/dashboard.py`  
**用途：** Web 可视化仪表盘，展示分析任务和报告  
**行数：** ~400 行

#### 核心逻辑

1. **暗色主题 CSS：** 自定义 GitHub 风格暗色主题
2. **两种扫描模式：** GitHub 仓库扫描 + 本地目录扫描
3. **自动轮询：** pending 任务每 2 秒轮询状态，60 秒超时
4. **Demo 数据加载：** 启动时自动加载 `/demo` 端点数据
5. **报告预览：** 漏洞摘要卡片 + 详细表格 + 逐条详情展开
6. **本地仓库检测：** 自动扫描 `/repos/` 目录

#### 关键功能模块

| 模块 | 功能 |
|------|------|
| `poll_pending_tasks()` | 轮询 pending 任务状态，自动获取报告 |
| `load_demo_data()` | 启动时加载 Demo 数据 |
| `get_local_repos()` | 检测 `/repos/` 下的本地仓库 |
| 侧边栏 | API 状态、快速链接 |
| 提交表单 | GitHub/Local 两种模式 |
| 报告预览 | 摘要卡片 + Findings 表格 + 详情展开 |

#### 与其他文件的关系

- **依赖：** `app/main.py` 的 API 端点（通过 HTTP 调用）
- **被依赖：** Dockerfile 通过 `streamlit run app/dashboard.py` 启动

---

### 3.6 app/demo_fixture.py — Demo 数据

**文件路径：** `app/demo_fixture.py`  
**用途：** 预置的示例扫描结果，评委无需配置即可体验  
**行数：** ~120 行

#### 核心内容

`DEMO_REPORT` 字典包含完整的示例报告：

- **9 个漏洞发现：** 1 Critical + 2 High + 3 Medium + 2 Low + 1 Info
- **漏洞类型覆盖：** SQL Injection、Command Injection、Hardcoded Secret、XSS、Insecure Deserialization、Path Traversal、CSRF、Debug Mode、Outdated Dependency
- **分析元数据：** 4.2 秒扫描、4 个 Agent、23 个文件、1847 行代码

#### 关键代码片段

```python
DEMO_REPORT = {
    "task_id": "cr-demo-20260820-001",
    "status": "completed",
    "summary": {"critical": 1, "high": 2, "medium": 3, "low": 2, "info": 1, "total": 9},
    "findings": [
        {"id": "CR-001", "severity": "critical", "category": "SQL Injection", ...},
        # ... 共 9 个
    ],
    "analysis": {
        "duration_seconds": 4.2,
        "agents_used": 4,
        "llm_model": "Qwen2.5-Coder-7B-Instruct (local)",
    },
}
```

#### 与其他文件的关系

- **被依赖：** `app/main.py` 的 `/demo` 端点导入 `DEMO_REPORT`
- **被依赖：** `app/dashboard.py` 的 `load_demo_data()` 调用 `/demo` API

---

### 3.7 app/nutrient_client.py — Nutrient PDF 客户端

**文件路径：** `app/nutrient_client.py`  
**用途：** 集成 Nutrient DWS API，将漏洞报告转为专业 PDF  
**行数：** ~300 行（含大量 HTML 模板）

#### 核心逻辑

1. **HTML 模板渲染：** `REPORT_HTML_TEMPLATE` 定义了专业级 PDF 样式（A4 排版、严重度卡片、漏洞详情卡片、数字签名区域）
2. **PDF 生成：** 调用 Nutrient DWS `/build` API，将 HTML 转为 PDF
3. **数字签名：** SHA-256 完整性校验（当前为哈希嵌入，预留 .pfx 证书签名）
4. **优雅降级：** API Key 未配置时跳过 PDF 生成，不影响其他报告格式

#### 关键方法

| 方法 | 用途 |
|------|------|
| `generate_pdf()` | 调用 Nutrient API 生成 PDF |
| `sign_pdf()` | SHA-256 签名（当前返回原 bytes + 哈希嵌入） |
| `generate_viewer_url()` | 预留 DWS Viewer 在线预览 |
| `_render_html()` | 将报告数据渲染为 HTML |

#### 与其他文件的关系

- **依赖：** `app/config.py`（NUTRIENT_API_KEY、NUTRIENT_API_URL）
- **被依赖：** `app/tasks.py` 的 `_generate_report()` 调用

---

## 四、核心引擎（code-risk-agent/）

`code-risk-agent/` 是一个 Git 子模块（`https://github.com/a9320/code-risk-agent.git`），包含完整的代码安全分析引擎。

### 4.1 core/models.py — 核心数据模型

**文件路径：** `code-risk-agent/core/models.py`  
**用途：** 定义引擎层所有数据流模型  
**行数：** ~200 行

#### 核心模型

| 模型 | 用途 |
|------|------|
| `Language` | 语言枚举：C / Python / Unknown |
| `Severity` | 严重度：info / low / medium / high / critical |
| `Confidence` | 置信度：low / medium / high |
| `CodeFile` | 源码文件（path, content, language, hash, line_count） |
| `AnalysisRequest` | 分析请求（files, rules, depth, enable_ai） |
| `Evidence` | 证据（source, rule_id, snippet, reasoning） |
| `Risk` | 风险项（id, title, severity, confidence, cwe_id, evidence, suggestion） |
| `AgentMessage` | Agent 间通信消息 |
| `AnalysisResult` | 完整分析结果（risks, timings, model_used） |
| `LLMConfig` | LLM 配置（backend, model_path, temperature, max_tokens） |
| `SemanticResponse` | Agent 2 LLM 输出 schema |
| `ReflectionResponse` | Agent 3 自反思 LLM 输出 schema |

#### 关键代码片段

```python
class CodeFile(BaseModel):
    path: Path
    content: str
    language: Language = Language.UNKNOWN

    @classmethod
    def from_path(cls, path: Path) -> CodeFile:
        content = path.read_text(encoding="utf-8", errors="replace")
        lang = _detect_language(path)
        return cls(path=path, content=content, language=lang)

class Risk(BaseModel):
    id: str
    title: str
    description: str
    severity: Severity
    confidence: Confidence
    cwe_id: Optional[str] = None
    language: Language
    file_path: Path
    line_start: int
    line_end: int
    evidence: list[Evidence] = Field(default_factory=list)
    suggestion: str
```

#### 与其他文件的关系

- **被依赖：** 引擎层所有模块（agents/、orchestrator.py）和 API 层的 `app/tasks.py`

---

### 4.2 core/llm_client.py — LLM 客户端

**文件路径：** `code-risk-agent/core/llm_client.py`  
**用途：** 统一的 LLM 推理客户端，支持本地 HTTP 和 llama-cpp-python 两种后端  
**行数：** ~300 行

#### 核心逻辑

1. **两种后端：**
   - `LOCAL_HTTP`：调用 llama-server HTTP API
   - `LOCAL_LLAMA_CPP`：直接加载 GGUF 模型（llama-cpp-python）
2. **自动重试：** 3 次重试，指数退避
3. **JSON 提取：** `_extract_json()` 多策略 JSON 解析（直接解析 → 代码块提取 → 括号匹配 → 截断修复）
4. **流式输出：** HTTP 后端支持 streaming
5. **GPU 检测：** 自动检测 AMD ROCm GPU，降级到 CPU

#### 关键方法

| 方法 | 用途 |
|------|------|
| `chat()` | 发送聊天请求，支持重试 |
| `chat_json()` | 发送请求并解析 JSON，支持 Pydantic schema 验证 |
| `_chat_http()` | HTTP API 调用 |
| `_chat_local()` | llama-cpp-python 直接推理 |
| `_chat_stream()` | 流式 HTTP 调用 |
| `_messages_to_prompt()` | OpenAI 格式 → Qwen2.5 ChatML 格式转换 |
| `_extract_json()` | 多策略 JSON 提取 |

#### 关键代码片段

```python
# 多策略 JSON 提取
def _extract_json(text: str) -> dict:
    # 策略 1: 直接解析
    try: return json.loads(text)
    except: pass
    # 策略 2: ```json 代码块提取
    if "```json" in text: ...
    # 策略 3: 括号深度匹配
    depth = 0; start = -1
    for i, ch in enumerate(text): ...
    # 策略 4: 截断修复（补全括号）
    if stripped.startswith("{"):
        opens = stripped.count("{"); closes = stripped.count("}")
        if opens > closes: repair = stripped + "}" * (opens - closes)
```

#### 与其他文件的关系

- **依赖：** `core/models.py`（LLMConfig、LLMBackend）
- **被依赖：** `app/tasks.py`（懒加载）、`agents/semantic_analyzer.py`、`agents/deep_verifier.py`、`orchestrator.py`

---

### 4.3 core/taint_analyzer.py — 污点分析器

**文件路径：** `code-risk-agent/core/taint_analyzer.py`  
**用途：** 追踪不可信数据从 source 到 sink 的数据流  
**行数：** ~200 行

#### 核心逻辑

1. **Source 定义：** C（argv, getenv, scanf, fgets, read, recv）；Python（input, sys.argv, request.args, os.environ）
2. **Sink 定义：** C（system, exec, strcpy, strcat, sprintf, printf, gets）；Python（eval, exec, os.system, subprocess, pickle.loads, yaml.load）
3. **变量追踪：** 正则匹配变量赋值，追踪被污染的变量
4. **直接流检测：** 同一行内 source→sink 的直接数据流

#### 关键类

| 类/方法 | 用途 |
|---------|------|
| `TaintFlow` | 数据流记录（source, sink, cwe_id, severity, path） |
| `TaintAnalyzer` | 主分析器 |
| `analyze_c()` | 分析 C 代码污点流 |
| `analyze_python()` | 分析 Python 代码污点流 |
| `_check_direct_flow()` | 检测直接 source→sink 流 |

#### 与其他文件的关系

- **被依赖：** `app/tasks.py` 的 `_run_taint_analysis()`、`orchestrator.py`

---

### 4.4 core/dependency_scanner.py — 依赖扫描器

**文件路径：** `code-risk-agent/core/dependency_scanner.py`  
**用途：** 扫描项目依赖文件中的已知漏洞（100% 离线）  
**行数：** ~250 行

#### 核心逻辑

1. **本地 OSV 数据：** 优先查询预下载的 OSV SQLite 数据库
2. **本地回退字典：** OSV 数据不可用时使用内置的 `_LOCAL_VULN_DB`（覆盖 Django、Flask、requests、PyYAML、lodash、express、axios 等常见包）
3. **版本比较：** `_parse_version()` + `_version_below()` + `_version_in_ranges()`
4. **多格式支持：** requirements.txt、pyproject.toml、package.json

#### 关键函数

| 函数 | 用途 |
|------|------|
| `scan_project_dependencies()` | 主入口，扫描项目目录 |
| `scan_requirements_txt()` | 扫描 Python requirements.txt |
| `scan_package_json()` | 扫描 Node.js package.json |
| `_query_local_osv()` | 查询本地 OSV 数据库 |
| `_version_in_ranges()` | 检查版本是否在漏洞范围内 |

#### 与其他文件的关系

- **被依赖：** `app/tasks.py` 的 `_run_dependency_scan()`、`orchestrator.py` 的 Phase 1.5

---

### 4.5 core/cve_client.py — CVE 数据库客户端

**文件路径：** `code-risk-agent/core/cve_client.py`  
**用途：** 查询本地 NVD SQLite 数据库的 CVE 信息（100% 离线）  
**行数：** ~150 行

#### 核心逻辑

1. **本地 SQLite 数据库：** 由 `scripts/download_cve_data.py` 预构建
2. **CWE→CVE 查询：** 按 CWE ID 查询关联的 CVE 记录
3. **缓存：** 内存缓存避免重复查询
4. **优雅降级：** 数据库不存在时返回空结果

#### 关键方法

| 方法 | 用途 |
|------|------|
| `query_by_cwe()` | 按 CWE ID 查询 CVE 列表 |
| `has_known_exploits()` | 检查是否有高危 CVE（CVSS ≥ 7.0） |
| `get_cve_summary()` | 获取 CWE 的 CVE 摘要文本 |
| `get_stats()` | 获取数据库统计信息 |

#### 与其他文件的关系

- **被依赖：** `agents/deep_verifier.py`（Agent 3 交叉验证）、`app/tasks.py`

---

### 4.6 core/memory.py — 记忆层

**文件路径：** `code-risk-agent/core/memory.py`  
**用途：** 双记忆系统——正确记忆（确认的漏洞模式）+ 错误记忆（误报模式）  
**行数：** ~250 行

#### 核心逻辑

1. **正确记忆：** 存储确认的漏洞模式，多次出现后自动提升置信度
2. **错误记忆：** 存储误报模式，出现 ≥2 次后自动抑制
3. **模式哈希：** `_hash_risk_pattern()` 将代码模式归一化（变量名→`<VAR>`，字符串→`<STR>`），使相似模式匹配
4. **磁盘持久化：** JSON 文件存储，`fcntl.flock` 文件锁保证并发安全

#### 关键类/方法

| 类/方法 | 用途 |
|---------|------|
| `MemoryEntry` | 单条记忆（pattern_hash, cwe_id, severity, source_count） |
| `MemoryLayer` | 记忆层主类 |
| `store_correct()` | 存储确认漏洞模式 |
| `store_error()` | 存储误报模式 |
| `recall()` | 查询匹配的记忆（错误记忆优先，抑制已知误报） |
| `_hash_risk_pattern()` | 归一化代码模式哈希 |

#### 关键代码片段

```python
def _hash_risk_pattern(self, risk: Risk) -> str:
    # 归一化：变量名→<VAR>，字符串→<STR>
    normalized = re.sub(r'\b[a-zA-Z_]\w*\b', _replace, code_pattern)
    normalized = re.sub(r'"[^"]*"', '<STR>', normalized)
    key = f"{risk.cwe_id}:{risk.language}:{normalized}"
    return hashlib.sha256(key.encode()).hexdigest()[:16]
```

#### 与其他文件的关系

- **被依赖：** `agents/deep_verifier.py`（Agent 3 验证时调用 recall/store）、`app/tasks.py`

---

### 4.7 core/semgrep_runner.py — Semgrep 运行器

**文件路径：** `code-risk-agent/core/semgrep_runner.py`  
**用途：** 封装 Semgrep CLI，扫描文件并转换为 Risk 对象  
**行数：** ~100 行

#### 核心逻辑

1. **调用 Semgrep CLI：** `semgrep scan --json --config=<rules> <file>`
2. **结果转换：** Semgrep JSON → Risk 对象（含 severity 映射：ERROR→HIGH, WARNING→MEDIUM, INFO→LOW）
3. **优雅降级：** Semgrep 未安装时返回空结果

#### 关键函数

| 函数 | 用途 |
|------|------|
| `run_semgrep()` | 运行 Semgrep 扫描单个文件 |
| `semgrep_to_risks()` | Semgrep JSON → Risk 对象转换 |
| `analyze_with_semgrep()` | 完整流水线：运行 + 转换 |

#### 与其他文件的关系

- **依赖：** `core/models.py`
- **被依赖：** `orchestrator.py` 的 Phase 2 并行执行

---

### 4.8 core/attack_knowledge.py — ATT&CK 知识库

**文件路径：** `code-risk-agent/core/attack_knowledge.py`  
**用途：** MITRE ATT&CK 技术到 CWE 漏洞的映射知识库  
**行数：** ~200 行

#### 核心逻辑

1. **ATT&CK 技术映射：** 15 个 ATT&CK 技术（T1059、T1190、T1055 等）→ CWE 映射
2. **反向映射：** CWE → ATT&CK 技术 ID
3. **合规框架引用：** OWASP Top 10、NIST CSF 关联

#### 关键函数

| 函数 | 用途 |
|------|------|
| `get_attack_context()` | 获取 CWE 的 ATT&CK 上下文 |
| `get_attack_description()` | 获取可读的攻击描述 |
| `get_compliance_references()` | 获取合规框架引用（OWASP、NIST） |

#### 与其他文件的关系

- **被依赖：** `agents/report_generator.py`（报告中嵌入 ATT&CK 和合规引用）

---

### 4.9 core/retry.py — 重试策略

**文件路径：** `code-risk-agent/core/retry.py`  
**用途：** 统一的重试装饰器，指数退避  
**行数：** ~50 行

#### 核心逻辑

`@retry` 装饰器，支持配置最大重试次数、基础延迟、最大延迟、目标异常类型、重试回调。

```python
@retry(max_retries=3, base_delay=1.0, exceptions=(ConnectionError,))
def unstable_operation(): ...
```

#### 与其他文件的关系

- **被依赖：** 可被任何需要重试逻辑的模块使用（目前主要在 `core/` 内部）

---

### 4.10 agents/static_analyzer.py — Agent 1 静态分析器

**文件路径：** `code-risk-agent/agents/static_analyzer.py`  
**用途：** 基于正则模式匹配的静态代码分析  
**行数：** ~500 行  
**这是项目中最大的单个文件，包含大量漏洞检测规则。**

#### 核心逻辑

1. **C 危险函数检测：** 8 个危险函数（gets, strcpy, strcat, sprintf, scanf, system, memcpy, popen）
2. **C 漏洞模式：** malloc 未检查 NULL、double free 检测
3. **Python 危险调用：** 10 个危险函数（eval, exec, pickle.loads, os.system, yaml.load 等）
4. **Python 漏洞模式：** 文件写入未验证、assert 用于安全检查
5. **新 C 模式：** 路径遍历（argv→fopen）、弱加密（MD5/DES/RC4）、整数溢出（atoi）
6. **新 Python 模式：** 硬编码密码、硬编码 secret/token
7. **Flask/Web 模式（Day 7 新增）：** SQL 注入（f-string/%s/+拼接）、SSTI（render_template_string）、XSS（|safe）、路径遍历（send_file）、命令注入（shell=True）、硬编码 SECRET_KEY、DEBUG=True、CORS 通配符、SSRF、CSRF

#### 关键类/方法

| 类/方法 | 用途 |
|---------|------|
| `StaticAnalyzer` | 主分析器类 |
| `analyze()` | 分析单个文件 |
| `analyze_batch()` | 批量分析多个文件 |
| `_analyze_c()` | C 代码分析（危险函数 + 漏洞模式 + 新模式） |
| `_analyze_python()` | Python 代码分析（危险调用 + 漏洞模式 + 新模式 + Flask 模式） |
| `_make_risk()` | 创建 Risk 对象（线程安全的 ID 自增） |

#### 检测规则统计

| 类别 | 规则数 |
|------|--------|
| C 危险函数 | 8 |
| C 漏洞模式 | 2 |
| C 新模式 | 3 |
| Python 危险调用 | 10 |
| Python 漏洞模式 | 2 |
| Python 新模式 | 2 |
| Flask/Web 模式 | 19 |
| **合计** | **46** |

#### 与其他文件的关系

- **依赖：** `core/models.py`
- **被依赖：** `app/tasks.py`、`orchestrator.py`

---

### 4.11 agents/semantic_analyzer.py — Agent 2 语义分析器

**文件路径：** `code-risk-agent/agents/semantic_analyzer.py`  
**用途：** LLM 驱动的语义代码分析，验证和增强静态分析结果  
**行数：** ~200 行

#### 核心逻辑

1. **风险验证：** 将 Agent 1 的发现发送给 LLM，判断 TRUE/FALSE positive
2. **新风险发现：** LLM 独立扫描代码，发现静态分析遗漏的漏洞
3. **攻击场景分析：** LLM 为每个风险生成攻击场景和影响评估
4. **严重度调整：** LLM 可建议调整风险严重度
5. **结构化输出：** 使用 Pydantic schema (`SemanticResponse`) 约束 LLM 输出

#### 关键方法

| 方法 | 用途 |
|------|------|
| `analyze()` | 主分析入口（验证已有风险 + 发现新风险） |
| `_build_prompt()` | 构建分析提示词 |
| `_merge_results()` | 合并 LLM 结果与已有风险 |
| `_scan_for_new_risks()` | 无静态风险时让 LLM 独立扫描 |

#### 关键代码片段

```python
SYSTEM_PROMPT = """You are a senior code security auditor...
For EACH risk, follow this reasoning chain:
1. Context Analysis: What is the function doing?
2. Input Tracing: Where does the data come from?
3. Validation Check: Is there input validation?
4. Exploitability: Can an attacker trigger this?
5. Classification: TRUE POSITIVE or FALSE POSITIVE?
"""
```

#### 与其他文件的关系

- **依赖：** `core/llm_client.py`、`core/models.py`
- **被依赖：** `app/tasks.py`、`orchestrator.py`

---

### 4.12 agents/deep_verifier.py — Agent 3 深度验证器

**文件路径：`code-risk-agent/agents/deep_verifier.py`  
**用途：** 三重交叉验证 + 自反思循环  
**行数：** ~250 行

#### 核心逻辑

1. **工具交叉验证：** 检查风险是否有 Semgrep / Pattern Match / LLM 多源确认
2. **知识库交叉验证：** CVE/NVD 数据库查询，确认 CWE 有真实 CVE 记录
3. **记忆验证：** 查询 MemoryLayer，抑制已知误报，增强已知真报
4. **自反思循环：** Agent 3 用 LLM 重新审查代码，发现前序 Agent 遗漏的风险（最多 2 轮迭代）

#### 关键方法

| 方法 | 用途 |
|------|------|
| `verify_batch()` | 批量验证所有风险 |
| `_verify_single_risk()` | 单个风险的三重交叉验证 |
| `_calculate_confidence()` | 根据确认次数计算置信度 |
| `_reflect_on_file()` | LLM 自反思，发现遗漏风险 |

#### 关键代码片段

```python
# 三重交叉验证
confirmations = 0
if has_semgrep: confirmations += 1      # 工具确认
if has_pattern: confirmations += 1      # 模式匹配确认
if has_ai: confirmations += 1           # LLM 确认
# CVE 数据库确认
if risk.cwe_id and cve_summary != "No CVE data":
    confirmations += 1

# 自反思循环（迭代发现遗漏）
for round_num in range(self.max_reflection_rounds):
    missed = self._reflect_on_file(f, current_risks)
    if not missed: break
    all_new_risks.extend(missed)
```

#### 与其他文件的关系

- **依赖：** `core/llm_client.py`、`core/memory.py`、`core/cve_client.py`、`core/models.py`
- **被依赖：** `app/tasks.py`、`orchestrator.py`

---

### 4.13 agents/report_generator.py — Agent 4 报告生成器

**文件路径：** `code-risk-agent/agents/report_generator.py`  
**用途：** 生成多格式审计报告  
**行数：** ~400 行

#### 核心逻辑

1. **JSON 报告：** 结构化数据，含外部引用（CWE URL、CVE URL、ATT&CK、合规框架）
2. **Markdown 报告：** 人类可读格式，含表格、链接、修复建议
3. **SARIF 2.1.0 报告：** OASIS 标准格式，兼容 GitHub Code Scanning、VS Code、Azure DevOps
4. **Rich 终端输出：** 彩色表格 + 风险树 + 性能摘要
5. **文件保存：** 支持 json/md/sarif 格式保存

#### 关键方法

| 方法 | 用途 |
|------|------|
| `generate_json()` | 生成 JSON 报告（含 ATT&CK + 合规引用） |
| `generate_markdown()` | 生成 Markdown 报告 |
| `generate_sarif()` | 生成 SARIF 2.1.0 报告 |
| `print_terminal()` | Rich 终端彩色输出 |
| `save_report()` | 保存报告到文件 |

#### 与其他文件的关系

- **依赖：** `core/models.py`、`core/attack_knowledge.py`
- **被依赖：** `orchestrator.py`（直接调用）、`app/tasks.py`（通过 `_to_sarif()` 自行实现简化版）

---

### 4.14 orchestrator.py — 编排器

**文件路径：** `code-risk-agent/orchestrator.py`  
**用途：** 状态机驱动的分析流水线编排器  
**行数：** ~350 行

#### 核心逻辑

1. **状态机：** INIT → PARSE → ANALYZE → VERIFY → REPORT → DONE/ERROR
2. **并行执行：**
   - Phase 1: Agent 1 静态分析（ThreadPoolExecutor, max_workers=4）
   - Phase 2: Semgrep + 污点分析（并行）
   - Phase 3: Agent 2 LLM 语义分析（max_workers=1，GPU 限制）
3. **性能追踪：** 每个阶段记录耗时（ms）
4. **项目根目录探测：** `_find_project_root()` 向上查找 markers（requirements.txt、package.json 等）

#### 状态转换图

```
INIT ──→ PARSE ──→ ANALYZE ──→ VERIFY ──→ REPORT ──→ DONE
  │        │         │           │          │
  └────────┴─────────┴───────────┴──────────┴──→ ERROR
```

#### 关键方法

| 方法 | 用途 |
|------|------|
| `run()` | 执行完整分析流水线 |
| `_validate_files()` | 过滤有效文件 |
| `_find_project_root()` | 探测项目根目录 |
| `_transition()` | 状态转换验证 |

#### 与其他文件的关系

- **依赖：** 所有 agents/ 和 core/ 模块
- **被依赖：** `code-risk-agent/main.py`（CLI 入口调用）

---

### 4.15 main.py — CLI 入口

**文件路径：** `code-risk-agent/main.py`  
**用途：** CodeRisk Agent 的命令行入口  
**行数：** ~150 行

#### 核心逻辑

1. **三个命令：** `analyze <path>`（分析代码）、`demo`（运行演示）、`info`（显示配置）
2. **文件收集：** `collect_files()` 递归收集 .c/.h/.py 文件
3. **Demo 模式：** 内置 vulnerable.c 和 server.py 示例代码
4. **选项：** `--no-ai`（禁用 LLM）、`--semgrep-config`（Semgrep 规则）、`--output`（输出格式）

#### 关键代码片段

```python
BANNER = r"""[bold cyan]
 ██████╗ ██████╗ ██████╗ ███████╗    ██████╗ ██╗███████╗██╗  ██╗
██╔════╝██╔═══██╗██╔══██╗██╔════╝    ██╔══██╗██║██╔════╝██║ ██╔╝
...
[/]"""

VERSION = "0.3.2"
```

#### 与其他文件的关系

- **依赖：** `orchestrator.py`、`core/models.py`、`core/llm_client.py`

---

## 五、配置与部署

### 5.1 Dockerfile

**文件路径：** `Dockerfile`  
**用途：** 统一 Docker 镜像，支持 API / Worker / Dashboard 三种模式  
**行数：** ~40 行

#### 关键特性

- **基础镜像：** `python:3.12-slim`
- **系统依赖：** git（clone 仓库）、gcc/build-essential（编译 Python 包）
- **CVE 数据库预构建：** 构建时运行 `download_cve_data.py`
- **环境变量：** PYTHONPATH、REDIS_URL、CODERISK_PATH、WORKER_CONCURRENCY
- **健康检查：** 每 30 秒检查 `/health`
- **默认启动：** uvicorn API（docker-compose 会覆盖为 worker/dashboard）

### 5.2 docker-compose.yml

**文件路径：** `docker-compose.yml`  
**用途：** 四服务编排  
**行数：** ~60 行

#### 服务架构

```
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│  Redis   │◄────│   API    │◄────│  Worker  │     │Dashboard │
│  :6379   │     │  :8000   │     │ (Celery) │     │  :8501   │
└──────────┘     └──────────┘     └──────────┘     └──────────┘
                   │                  │                  │
                   └──────────────────┴──────────────────┘
                              共享 volumes:
                            ./reports, ./repos
```

| 服务 | 镜像 | 端口 | 说明 |
|------|------|------|------|
| `redis` | redis:7-alpine | 6379 | 消息队列 + 结果存储 |
| `api` | 本地构建 | 8000 | FastAPI + Uvicorn（--reload） |
| `worker` | 本地构建 | - | Celery Worker |
| `dashboard` | 本地构建 | 8501 | Streamlit 仪表盘 |

### 5.3 docker-compose.gpu.yml

**文件路径：** `docker-compose.gpu.yml`  
**用途：** GPU 覆盖层，启用 AMD ROCm GPU 加速  
**使用方式：** `docker-compose -f docker-compose.yml -f docker-compose.gpu.yml up`

#### 关键配置

- **设备映射：** `/dev/kfd` + `/dev/dri`
- **用户组：** `video`
- **环境变量：** `HSA_OVERRIDE_GFX_VERSION`、`HIP_VISIBLE_DEVICES`、`GGML_HIP=ON`

### 5.4 requirements.txt

**文件路径：** `requirements.txt`  
**用途：** Python 依赖清单

```
# API 层
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
celery[redis]>=5.4.0
redis>=5.0.0
pydantic>=2.0.0
httpx>=0.27.0
python-multipart>=0.0.9
streamlit>=1.30.0

# 引擎层
rich>=13.0
tree-sitter>=0.21
semgrep>=1.0
```

### 5.5 .env.example

**文件路径：** `.env.example`  
**用途：** 环境变量模板

| 变量 | 说明 | 必填 |
|------|------|------|
| `REDIS_URL` | Redis 连接 URL | 否（有默认值） |
| `CODERISK_API_KEY` | API 认证密钥 | 是 |
| `CODERISK_PATH` | 引擎路径 | 否（自动探测） |
| `NUTRIENT_DWS_API_KEY` | Nutrient PDF API Key | 否（PDF 功能禁用） |
| `NUTRIENT_DWS_API_URL` | Nutrient API URL | 否 |
| `GITHUB_WEBHOOK_SECRET` | Webhook 签名密钥 | 否 |
| `REPORTS_DIR` | 报告存储目录 | 否 |
| `WORKER_CONCURRENCY` | Worker 并发数 | 否 |

### 5.6 .gitmodules

**文件路径：** `.gitmodules`  
**用途：** Git 子模块配置

```ini
[submodule "code-risk-agent"]
    path = code-risk-agent
    url = https://github.com/a9320/code-risk-agent.git
```

---

## 六、CI/CD

### 6.1 .github/workflows/ci.yml

**文件路径：** `.github/workflows/ci.yml`  
**用途：** 主 CI 流水线，包含三个 Job  
**行数：** ~180 行

#### Job 1: test（单元测试 + 引擎测试）

- **触发条件：** push/PR 到 main/master/dev
- **服务：** Redis 7 Alpine
- **步骤：**
  1. Checkout（含子模块）
  2. Python 3.12 + pip 缓存
  3. 安装依赖 + pytest
  4. 运行 Cloud 层测试（`pytest tests/`）
  5. 运行 Engine 层测试（`cd code-risk-agent && pytest tests/`）
  6. 生成覆盖率报告 + 上传 Codecov

#### Job 2: e2e（Docker Compose 端到端验证）

- **步骤：**
  1. Docker Buildx + 缓存
  2. `docker compose build`
  3. `docker compose up -d` + 等待健康检查
  4. 测试 /health、/demo 端点
  5. 运行 verify.sh
  6. 测试 GitHub 分析提交
  7. 导出镜像为 artifact

#### Job 3: security（安全扫描）

- **依赖：** e2e Job 完成后
- **步骤：**
  1. pip-audit（Python 依赖漏洞扫描）
  2. 下载 e2e 构建的镜像
  3. Trivy 容器漏洞扫描
  4. 上传 SARIF 结果到 GitHub Security tab

### 6.2 .github/workflows/docker-build.yml

**文件路径：** `.github/workflows/docker-build.yml`  
**用途：** 版本标签触发的 Docker 镜像构建和推送  
**行数：** ~25 行

- **触发条件：** push tag `v*`
- **目标：** GHCR（GitHub Container Registry）
- **标签：** `latest` + 版本号

---

## 七、测试

### 7.1 tests/conftest.py

**文件路径：** `tests/conftest.py`  
**用途：** Pytest 共享 fixtures  
**行数：** ~10 行

```python
@pytest.fixture
def client() -> TestClient:
    return TestClient(app)
```

### 7.2 tests/test_health.py

**文件路径：** `tests/test_health.py`  
**用途：** 公开端点测试  
**行数：** ~30 行

| 测试 | 验证内容 |
|------|----------|
| `test_root_returns_200` | 根路由返回 200 |
| `test_health_returns_200_with_status` | /health 返回 200 或 503（降级），包含 status 字段 |
| `test_docs_returns_200` | /docs 返回 HTML |
| `test_demo_returns_200_with_9_findings` | /demo 返回 9 个 findings |

### 7.3 tests/test_demo.py

**文件路径：** `tests/test_demo.py`  
**用途：** Demo 数据一致性测试  
**行数：** ~25 行

| 测试 | 验证内容 |
|------|----------|
| `test_demo_findings_have_required_fields` | 每个 finding 包含 id/severity/category/file/line/description |
| `test_demo_findings_sorted_by_severity` | findings 按严重度降序排列 |

### 7.4 tests/test_auth.py

**文件路径：** `tests/test_auth.py`  
**用途：** 认证测试  
**行数：** ~35 行

| 测试 | 验证内容 |
|------|----------|
| `test_missing_auth_returns_401` | 无 Authorization 头返回 401 |
| `test_invalid_key_returns_403` | 错误 API Key 返回 403 |
| `test_valid_key_returns_200_or_422` | 正确 API Key 通过认证 |
| `test_task_status_missing_auth_returns_401` | 任务状态端点需要认证 |
| `test_report_missing_auth_returns_401` | 报告端点需要认证 |

### 7.5 verify.sh

**文件路径：** `verify.sh`  
**用途：** 一键验证脚本（评委使用）  
**行数：** ~300 行

#### 验证项目（9 大类）

1. **服务健康检查** — /、/health
2. **认证授权** — 401/403/200 响应验证
3. **任务生命周期** — 创建任务、查询状态、agent_status 字段
4. **ZIP 上传安全** — 有效 ZIP 接受、非 ZIP 拒绝
5. **GitHub Webhook 安全** — 无签名拒绝
6. **报告隔离** — 不同 API Key 不能互相访问
7. **核心引擎** — 子模块、Agent 目录、测试、CVE 脚本
8. **文档完整性** — JUDGES.md、README、Docker Compose、Bruno
9. **环境验证** — .env、Python 版本、Redis 状态

---

## 八、API 端点汇总

| 端点 | 方法 | 认证 | 用途 |
|------|------|------|------|
| `/` | GET | 否 | 根路由信息 |
| `/health` | GET | 否 | 健康检查（Redis + Celery + GPU） |
| `/docs` | GET | 否 | Swagger UI |
| `/demo` | GET | 否 | Demo 数据（9 个示例漏洞） |
| `/api/v1/analyze` | POST | 是 | 提交 GitHub 仓库分析 |
| `/api/v1/analyze/upload` | POST | 是 | 上传 ZIP 文件分析 |
| `/api/v1/scan-local` | POST | 是 | 本地目录扫描 |
| `/api/v1/tasks/{task_id}` | GET | 是 | 查询任务状态 |
| `/api/v1/reports/{task_id}` | GET | 是 | 获取分析报告 |
| `/api/v1/reports/{task_id}/pdf` | GET | 是 | 下载 PDF 报告 |
| `/api/v1/webhooks/github` | POST | 签名验证 | GitHub Webhook |

---

## 九、数据模型汇总

### API 层模型（app/models.py）

| 模型 | 字段 |
|------|------|
| `AnalyzeRequest` | source, repo_url, branch, callback_url, output_formats |
| `AnalyzeResponse` | task_id, status, message |
| `TaskResponse` | task_id, status, progress, agent_status, created_at, updated_at, error |
| `ReportResponse` | task_id, summary, findings, report_urls, digital_signature, completed_at |
| `ErrorResponse` | error, detail |

### 引擎层模型（code-risk-agent/core/models.py）

| 模型 | 字段 |
|------|------|
| `CodeFile` | path, content, language, hash, line_count |
| `Risk` | id, title, description, severity, confidence, cwe_id, language, file_path, line_start, line_end, evidence, suggestion |
| `Evidence` | source, rule_id, snippet, line_start, line_end, reasoning |
| `AnalysisRequest` | files, rules, depth, enable_ai |
| `AnalysisResult` | request_id, files_analyzed, risks, analysis_time_ms, model_used, perf_timings |
| `LLMConfig` | backend, api_url, model, model_path, n_gpu_layers, temperature, max_tokens, timeout |
| `TaintFlow` | source, sink, source_line, sink_line, cwe_id, severity, description, confidence, path |
| `MemoryEntry` | pattern_hash, cwe_id, severity, title, description, source_count, memory_type |

---

## 十、配置项汇总

| 配置项 | 环境变量 | 默认值 | 影响范围 |
|--------|----------|--------|----------|
| Redis URL | `REDIS_URL` | `redis://localhost:6379/0` | API + Worker |
| API Key | `CODERISK_API_KEY` | `dev-key-change-in-production` | API 认证 |
| 引擎路径 | `CODERISK_PATH` | 自动探测 | Worker |
| Nutrient API Key | `NUTRIENT_DWS_API_KEY` | `""`（禁用） | PDF 生成 |
| Nutrient API URL | `NUTRIENT_DWS_API_URL` | `https://api.nutrient.io/build` | PDF 生成 |
| Webhook 密钥 | `GITHUB_WEBHOOK_SECRET` | `""`（跳过验证） | Webhook |
| 报告目录 | `REPORTS_DIR` | `./reports` | API + Worker |
| Worker 并发 | `WORKER_CONCURRENCY` | `2` | Worker |
| LLM 后端 | `LLM_BACKEND` | `local_llama_cpp` | 引擎 |
| 模型路径 | `CODERISK_MODEL_PATH` | 自动探测 | 引擎 |
| Semgrep 规则 | `SEMGREP_RULES` | `p/default` | 引擎 |

---

## 十一、模块关系图

### 文件依赖关系（简化）

```
app/main.py
├── app/config.py
├── app/models.py
├── app/tasks.py
│   ├── app/config.py
│   ├── app/nutrient_client.py
│   │   └── app/config.py
│   └── code-risk-agent/ (通过 PYTHONPATH)
│       ├── agents/static_analyzer.py → core/models.py
│       ├── agents/semantic_analyzer.py → core/llm_client.py, core/models.py
│       ├── agents/deep_verifier.py → core/llm_client.py, core/memory.py, core/cve_client.py
│       ├── core/taint_analyzer.py
│       ├── core/dependency_scanner.py
│       └── core/models.py
└── app/demo_fixture.py

code-risk-agent/orchestrator.py
├── agents/static_analyzer.py
├── agents/semantic_analyzer.py
├── agents/deep_verifier.py
├── agents/report_generator.py
├── core/llm_client.py
├── core/memory.py
├── core/cve_client.py
├── core/taint_analyzer.py
├── core/dependency_scanner.py
├── core/semgrep_runner.py
└── core/models.py

code-risk-agent/main.py
├── orchestrator.py
├── core/models.py
└── core/llm_client.py
```

---

## 十二、已知问题

### 1. static_analyzer.py 正则 bug

**位置：** `code-risk-agent/agents/static_analyzer.py`

**问题描述：**

`C_ASSIGN_PATTERN` 正则表达式在某些边缘情况下可能匹配失败：

```python
C_ASSIGN_PATTERN = re.compile(r"(?:(?:\w+\s*\*?\s+)?)(\w+)\s*=\s*(.+);")
```

- 对于多行赋值语句无法正确匹配
- 对于包含 `=` 的复杂表达式（如 `if (a = b)`）可能误匹配
- 类型声明中含指针的复杂形式（如 `char **argv = ...`）可能无法正确捕获变量名

**影响：** 污点分析中变量追踪可能遗漏某些赋值场景，导致数据流断裂。

**修复建议：** 使用 Tree-sitter AST 解析替代正则匹配，或增强正则以支持更多 C 语法变体。

### 2. LLM 客户端无云端 fallback

**位置：** `code-risk-agent/core/llm_client.py`

**问题描述：** 当本地 GGUF 模型未部署且 llama-server 未运行时，`LLMClient` 初始化会抛异常，导致语义分析（Agent 2）和深度验证（Agent 3 的自反思循环）被跳过。

**影响：** 在无 GPU 环境中，系统退化为纯静态分析模式，漏检率上升。

**当前缓解：** `app/tasks.py` 中通过 `try/except` 捕获，优雅降级。

### 3. Nutrient PDF 签名仅为哈希嵌入

**位置：** `app/nutrient_client.py` 的 `sign_pdf()`

**问题描述：** 当前 PDF "签名" 只是 SHA-256 哈希嵌入 HTML 模板，不是真正的数字证书签名（.pfx）。

**影响：** PDF 可被篡改而不被检测到。

**修复建议：** 接入 Nutrient Digital Signatures API + .pfx 证书。

### 4. Streamlit Dashboard 硬编码 API 地址

**位置：** `app/dashboard.py`

```python
API_BASE = "http://api:8000"
```

**问题：** 硬编码为 Docker 内部网络地址，本地开发时需要修改。

### 5. 依赖扫描器的本地漏洞数据库有限

**位置：** `code-risk-agent/core/dependency_scanner.py`

**问题：** `_LOCAL_VULN_DB` 只覆盖 9 个常见包，且版本阈值可能过时。

**缓解：** 优先使用 OSV 本地数据（需运行 `download_osv_data.py` 构建）。

### 6. 污点分析仅支持单函数

**位置：** `code-risk-agent/core/taint_analyzer.py`

**问题描述：** 当前实现只追踪单个函数内的变量赋值，不支持跨函数数据流分析。

**影响：** 如果用户输入通过函数参数传递到另一个函数的危险调用，污点分析会遗漏。

**文档说明：** 文件头部已注明 "Single-function variable tracking only — cross-function data flow requires Call Graph analysis (planned for future release)"。

### 7. Redis 连接池在 Celery Worker 中的复用

**位置：** `app/tasks.py`

```python
_redis_pool = redis.ConnectionPool.from_url(settings.REDIS_URL, max_connections=20)
```

**问题：** 模块级连接池在 Celery fork 后可能有状态问题。

**影响：** 极端并发下可能出现连接泄漏。

**缓解：** `max_connections=20` 提供了足够的缓冲。

---

## 十三、代码审查发现的 Bug（2026-08-24 审查）

以下为逐文件代码审查发现的实际 Bug，按严重程度排列。

### Bug 1（🔴 已修复）：static_analyzer.py 正则语法错误

**文件：** `code-risk-agent/agents/static_analyzer.py` 第 361、393 行

**原代码：**
```python
"pattern": r"SECRET_KEY\s*=\s*['\"][^'\"]+['\"]",
"pattern": r"@app\.route\s*\([^)]*methods\s*=\s*\[[^]]*['\"]POST['\"]",
```

**原因：** 在 Python 原始字符串 `r"..."` 中，`"`（反斜杠+引号）会阻止字符串正常终止。Python 解析器将 `['\"]` 中的 `\"` 视为转义引号，导致字符串延伸到下一个 `"` 才闭合，后续代码被吞入字符串内，产生 `SyntaxError: closing parenthesis ']' does not match opening parenthesis '{'`。

**影响：** `PYTHON_FLASK_PATTERNS`（20 条 Flask/Web 漏洞规则）所在的整个模块无法加载。静态分析退化为只检测 C 语言模式，Python 项目的扫描结果为 0 条。

**修复：** 改为非原始字符串，用 `\\s` 替代 `\s`：
```python
"pattern": "SECRET_KEY\\s*=\\s*['\"][^'\"]+['\"]",
```

**验证：** `ast.parse()` 通过 ✅

---

### Bug 2（🔴 已修复）：tasks.py _run_dependency_scan 字典当对象访问

**文件：** `app/tasks.py` `_run_dependency_scan` 函数

**原代码：**
```python
risks = scan_project_dependencies(Path(code_path))
for r in risks:
    sev = r.severity.value if hasattr(r.severity, "value") else str(r.severity)
    result.append({
        "id": r.id,
        "type": r.cwe_id or "CWE-1395",
        "title": r.title,
        ...
    })
```

**原因：** `scan_project_dependencies()` 返回 `list[dict]`（键：`package`, `version`, `cwe`, `description`, `fix`, `source`），但代码用属性访问（`r.severity.value`、`r.cwe_id`、`r.title`）而非字典访问（`r["cwe"]`、`r.get("package")`）。

**影响：** `AttributeError: 'dict' object has no attribute 'severity'`，依赖扫描直接崩溃返回空列表。requirements.txt 中的已知漏洞（如 Flask、Jinja2 等）永远不会被检出。

**修复：**
```python
for r in risks:
    pkg = r.get("package", "unknown")
    result.append({
        "id": f"DEP-{pkg.upper()}",
        "type": r.get("cwe", "CWE-1395"),
        "title": f"Vulnerable dependency: {pkg} {r.get('version', '')}".strip(),
        "severity": "high",
        "description": r.get("description", ""),
        "file": r.get("file", "requirements.txt"),
        "line": 0,
        "confidence": 50,
    })
```

---

### Bug 3（🟡 已修复）：tasks.py _run_verification confidence 硬编码

**文件：** `app/tasks.py` `_run_verification` 函数第 467 行

**原代码：**
```python
conf = Confidence.LOW  # 硬编码
```

**原因：** 所有 findings 的 confidence 被强制设为 `Confidence.LOW`，忽略了静态分析（80%）、污点分析（60-80%）等上游传入的原始置信度。

**影响：** 报告中所有漏洞的置信度都显示为 LOW，导致高置信度的漏洞（如 eval 注入 80%）和低置信度的漏洞无法区分，影响优先级判断。

**修复：** 根据百分比映射回枚举：
```python
conf_pct = f.get("confidence", 30)
if conf_pct >= 70:
    conf = Confidence.HIGH
elif conf_pct >= 40:
    conf = Confidence.MEDIUM
else:
    conf = Confidence.LOW
```

---

### Bug 4（🟡 未修复）：_scan_code_files 只支持 3 种语言

**文件：** `app/tasks.py` `_scan_code_files` 函数

**问题：** 文件扫描只处理 `.c`、`.h`、`.py` 后缀，JavaScript、TypeScript、Java、Go、Rust 等语言的文件全被跳过。

**影响：** 非 Python/C 项目的扫描结果为空，即使代码中存在明显漏洞。

**修复建议：** 扩展后缀列表，或使用 `code-risk-agent/core/models.py` 中的 `Language` 枚举和 `_detect_language()` 函数做统一语言检测。

---

### Bug 5（🟡 未修复）：nutrient_client.py sign_pdf 是空操作

**文件：** `app/nutrient_client.py` `sign_pdf` 方法

**问题：** 当前实现只是计算 SHA-256 hash 并打日志，直接返回原始 bytes，没有真正调用 Nutrient Digital Signatures API 进行证书签名。

**影响：** PDF 报告的「数字签名」功能形同虚设，PDF 可被篡改而不被检测到。

**修复建议：** 接入 Nutrient Digital Signatures API + .pfx 证书，或在文档中明确标注「完整性校验」而非「数字签名」。

---

### Bug 6（🟢 低风险）：hmac.compare_digest 非 ASCII 字符问题

**文件：** `app/main.py` `verify_api_key` 函数

**问题：** 如果 `CODERISK_API_KEY` 包含非 ASCII 字符（如 Unicode 省略号 `…`），`hmac.compare_digest()` 会抛出 `TypeError: comparing strings with non-ASCII characters is not supported`，导致 500 Internal Server Error。

**触发条件：** `.env` 中的 Key 包含非 ASCII 字符。开发环境中 `.env.example` 的占位符如果直接复制使用可能触发。

**修复建议：** 在 `verify_api_key` 中增加 ASCII 校验：
```python
token = authorization.replace("Bearer ", "").strip()
if not token.isascii():
    raise HTTPException(status_code=400, detail="API Key must contain only ASCII characters")
```

---

### 审查方法

1. 对所有 26 个 Python 文件执行 `ast.parse()` 语法检查
2. 对比 `tasks.py` 调用的方法签名与 `agents/` 和 `core/` 模块的实际定义
3. 追踪数据流：`scan_project_dependencies` → `_run_dependency_scan` → `_run_verification` → `_generate_report`
4. 检查 dict vs object 类型不匹配问题
5. 验证 CI/CD 配置与实际分支名、环境变量的一致性

---

> **文档结束** — 本文档覆盖了 CodeRisk Cloud 项目的所有源码文件，共计 30+ 个核心文件的完整分析，包含 13 个章节和 6 个已发现 Bug。
